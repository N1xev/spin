---
phase: 05-v2-0-universal-scaffolder-task-runner
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/new.go
  - cmd/new_charm.go
  - cmd/new_extras.go
  - internal/template/loader.go
  - internal/template/post_hook.go
  - internal/template/template.go
  - internal/ecosystem/registry.go
  - internal/ecosystem/registry_test.go
autonomous: true
requirements:
  - BC-01
  - BC-02
  - BC-03
  - TPL-12
  - TPL-13
  - TPL-15
  - TPL-16
  - TPL-17
  - TPL-18
  - ECO-07
  - ECO-08
  - ECO-09
  - ECO-10
user_setup: []

must_haves:
  truths:
    - "`spin new <name>` (no ecosystem) prints a one-time deprecation notice and routes to charm with output identical to the v1 path"
    - "`spin new charm <name> --template <user/repo>` clones the git repo, parses spin.toml, runs the huh form (or applies defaults in non-TTY), renders, runs post-hooks, and deletes spin.toml from the output"
    - "Unknown ecosystem names return a clear error listing known ecosystems"
    - "The `spin.toml` file is removed from the rendered output directory"
  artifacts:
    - path: "cmd/new.go"
      provides: "Deprecation notice + ecosystem dispatch when first positional matches a known ecosystem"
      contains: "deprecationPrinted"
    - path: "cmd/new_charm.go"
      provides: "Template loader wiring (merges template.Render with eco.Render)"
      contains: "template.Loader"
    - path: "internal/template/post_hook.go"
      provides: "Post-hook runner: renders {{.var}} in shell command, execs via sh -c"
      contains: "RunPostHook"
    - path: "internal/ecosystem/registry_test.go"
      provides: "Unit tests for Registry.Get, Names, Detect"
      contains: "TestRegistry_Get_UnknownEcosystem"
  key_links:
    - from: "cmd/new.go"
      to: "internal/ecosystem/registry.go"
      via: "defaultRegistry().Get(ecoName)"
      pattern: "defaultRegistry\\(\\)"
    - from: "cmd/new_charm.go"
      to: "internal/template/loader.go"
      via: "template.NewLoader(\"\").Load(ref)"
      pattern: "template\\.NewLoader"
    - from: "internal/template/loader.go"
      to: "git CLI"
      via: "exec.Command with GIT_TERMINAL_PROMPT=0 env"
      pattern: "GIT_TERMINAL_PROMPT=0"
---

<objective>
Wire the v2.0 ecosystem dispatch and external template loader end-to-end. Make `spin new <name>` (no ecosystem) print a one-time deprecation notice and route to charm. Make `spin new <ecosystem> <name> --template <ref>` clone a git repo, parse spin.toml, run the huh form, render, run post-hooks, and delete the spin.toml from output. This delivers Phase 5 success criteria 1 and 3.

Purpose: Without this plan, the universal claim is half-realized: ecosystems exist but the v1 path is still canonical, and the external template loader is wired in skeleton but never reached from any user-facing command.

Output: A modified `cmd/new.go` (legacy shim), upgraded `cmd/new_charm.go` (v2 path with template loader), an upgraded `internal/template/loader.go` (new cache dir), a new `internal/template/post_hook.go`, and unit tests for the registry. The build must remain green and ALL v1.0 commands/flags must still work.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-v2-0-universal-scaffolder-task-runner/05-CONTEXT.md

# Skeleton (READ before implementing)
@internal/template/loader.go
@internal/template/template.go
@internal/template/form.go
@internal/template/engine.go
@internal/template/spin_toml.go
@internal/ecosystem/registry.go
@internal/ecosystem/ecosystem.go
@cmd/new.go
@cmd/new_charm.go
@cmd/new_extras.go
@cmd/ecosystem.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: One-time deprecation notice + ecosystem-name detection in cmd/new.go</name>
  <files>cmd/new.go</files>
  <read_first>
    - cmd/new.go (current shim — modify, do not rewrite)
    - cmd/new_charm.go (the v2 charm dispatch to delegate to)
    - cmd/ecosystem.go (defaultRegistry to look up ecosystems)
    - internal/ecosystem/registry.go (Registry.Get + Names)
    - .planning/phases/05-v2-0-universal-scaffolder-task-runner/05-CONTEXT.md (decision J: legacy shim pattern)
  </read_first>
  <action>
Modify `cmd/new.go` to:
1. Add a package-level `var deprecationPrinted bool` (one-time guard).
2. In `runNew`, before the existing logic, add an ecosystem-name check:
   - If `len(args) == 0`: behave as today (legacy path).
   - If `len(args) >= 1` and the FIRST positional matches a known ecosystem name (case-insensitive): set `args = args[1:]` and re-run via the v2 dispatch (delegated to a new helper `dispatchV2(args, cmd)` — see below). Recognise known ecosystems by looking up `defaultRegistry().Names()`.
   - If `len(args) == 1` AND the first positional is NOT a known ecosystem: print the deprecation notice once and continue to the legacy path.
   - If `len(args) >= 2` AND the first positional is an UNKNOWN ecosystem name: return an error `"spin new: unknown ecosystem %q (available: %v)"`.
3. The deprecation notice: `fmt.Fprintf(os.Stderr, "WARN  ` + "`spin new <name>`" + ` is deprecated; use ` + "`spin new charm <name>`" + ` (or ` + "`spin new rust <name>`" + ` for cargo projects). The legacy form will be removed in v3.0.\n")`. Print it once per process (guarded by `deprecationPrinted`).
4. Add a `dispatchV2(args []string, cmd *cobra.Command) error` function in `cmd/new.go` that:
   - Sets `ecoName := args[0]`, `name := args[1]`.
   - Looks up `r := defaultRegistry(); eco, err := r.Get(ecoName)`.
   - Collects all flags from `cmd.Flags()` (using the same `cmd.Flags().VisitAll` pattern as `runNewCharm`) into a `map[string]any` named `flags`.
   - Builds `ecosystem.Context{Name: name, Year: time.Now().Year(), SpinVer: spinVersion(), Flags: flags}`.
   - Calls `eco.Validate(ctx)`, `eco.Render(ctx)`, `eco.PostScaffold(ctx, name)`.
   - Returns any error.
5. The `charm` and `rust` subcommands (declared in `cmd/new_charm.go` and `cmd/new_rust.go`) must continue to work as the canonical v2 entry points. This task does NOT remove them; it adds the legacy shim's new behaviour.

Use only `os`, `fmt`, `time`, `strings`, plus the existing internal imports. Do not add new dependencies.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && ./spin new myapp --tui --bubbletea --no-git --no-interactive 2>&1 | head -5 && echo SEP && ./spin madeup_eco name --no-git --no-interactive 2>&1 | head -5 && ls myapp 2>/dev/null | head -3</automated>
  </verify>
  <done>
    - `cmd/new.go` contains `var deprecationPrinted bool` and prints the deprecation notice on first call only.
    - `cmd/new.go` contains a `dispatchV2` helper.
    - `cmd/new.go` detects known vs unknown ecosystem names as the first positional arg.
    - `./spin new myapp --tui --bubbletea --no-git --no-interactive` succeeds, prints the deprecation notice once, and produces a `./myapp/` tree.
    - `./spin new madeup_eco name --no-interactive` exits non-zero with an error message that includes the list of known ecosystems.
    - A second `spin new <name>` invocation in the same process does NOT print the deprecation notice again (verify by running the binary twice in a row — each invocation is a new process, so the message prints each time; the per-process guard is testable via a `go test` of the deprecation helper).
    - `go build ./...` and `go vet ./...` pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Upgrade template loader — new cache dir, GIT_TERMINAL_PROMPT=0, post-hook runner, spin.toml deletion</name>
  <files>internal/template/loader.go, internal/template/post_hook.go, internal/template/template.go, cmd/new_charm.go, cmd/new_extras.go</files>
  <read_first>
    - internal/template/loader.go (the existing skeleton loader)
    - internal/template/template.go (Render + RenderTo methods)
    - internal/template/engine.go (writeFiles with path-traversal guard)
    - internal/template/spin_toml.go (SpinToml struct + PostHook)
    - internal/template/form.go (ResolveForm for huh interaction)
    - cmd/new_charm.go (the dispatcher that will invoke the loader)
    - cmd/new_extras.go (where --template is bound)
    - .planning/phases/05-v2-0-universal-scaffolder-task-runner/05-CONTEXT.md (decisions F and G)
  </read_first>
  <action>
1. Modify `internal/template/loader.go`:
   - Change `defaultCacheDir()` to return `~/.config/spin/templates/` (not `~/.cache/spin/templates/`). Use `os.UserConfigDir()` to find the XDG config dir; fall back to `$HOME/.config/spin/templates` on error.
   - In `cloneGit`, ensure `cmd.Env = append(os.Environ(), "GIT_TERMINAL_PROMPT=0")` (preserve parent env, add the env var).
   - Add an unexported helper `Lister() ([]string, error)` method that returns `[]string` of dir basenames under `CacheDir` (uses `os.ReadDir`). Used by tests.
   - Add an unexported helper `Clear(ref string) error` that removes the cached clone. Used by tests.

2. Add a new file `internal/template/post_hook.go`:
   - `RunPostHook(t *Template, values map[string]any, dir string) error`. Looks at `t.SpinToml.Post.Run`. If empty, returns nil. Otherwise:
     - Renders the post-hook command string with `text/template` against `values` (so `{{.project_name}}` interpolates correctly).
     - Runs the rendered command via `sh -c` in `dir` (use `os/exec`).
     - Returns any non-zero exit as an error.
   - The post-hook runs AFTER files are written, BEFORE `spin.toml` is deleted.

3. Modify `internal/template/template.go`:
   - Add a method `RenderToWithPost(dir string, values map[string]any) error` that: calls `Render`, calls `writeFiles`, runs the post-hook via `RunPostHook`, and finally DELETES `dir/spin.toml` AND walks the dir to delete any other `spin.toml` files (filepath.Walk). This satisfies TPL-16.
   - Reuse the path-traversal guard from `engine.writeFiles` unchanged.

4. Modify `cmd/new_charm.go` `runNewCharm`:
   - After building the `ecosystem.Context`, check `cmd.Flags().Changed("template")`. If yes and the value is non-empty:
     - Load: `tpl, err := template.NewLoader("").Load(templateRef)`.
     - Resolve form: `values, err := tpl.ResolveForm(map[string]any{"name": ctx.Name, "module": ctx.Module, "year": ctx.Year, "type": ctx.GetString("type")}, isTerminal())`.
     - Render template + ecosystem: `ecoFiles, _ := eco.Render(ctx); tplFiles, _ := tpl.Render(values); merged := mergeMaps(ecoFiles, tplFiles)`. Last-write-wins: tpl overrides eco.
     - Write `merged` to `ctx.Name/` and run the post-hook via `template.RunPostHook(tpl, values, ctx.Name)`. Then delete `ctx.Name/spin.toml`.
   - When `--template` is NOT set, the existing flow (eco.Render + eco.PostScaffold) runs unchanged.
   - Add unexported helper `mergeMaps(a, b map[string][]byte) map[string][]byte` in `cmd/new_charm.go`.

5. Modify `cmd/new_extras.go`:
   - Add a new flag binding: `newCmd.PersistentFlags().String("template", "", "external template: git URL or user/repo")` (for the legacy v1 `spin new <name> --template <ref>` form).
   - In the PreRunE block, if `cmd.Flags().Changed("template")` and the value is non-empty AND no ecosystem subcommand is in play, call `dispatchV2(args, cmd)` (from Task 1) and return its result. Use `os.Args` or `cobra.Command.Parent()` to detect subcommand presence.

Use only existing imports. The `internal/template` package will need `os/exec` and `text/template`; add these imports as needed.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/template/... && echo SEP && ./spin new foobar --no-git --no-interactive 2>&1 | head -5 && echo SEP && ls foobar/spin.toml 2>&1 || echo spin.toml_correctly_absent</automated>
  </verify>
  <done>
    - `internal/template/loader.go` uses `~/.config/spin/templates/` as cache dir.
    - `internal/template/loader.go` `cloneGit` includes `GIT_TERMINAL_PROMPT=0` in env.
    - `internal/template/post_hook.go` exists with `RunPostHook(t *Template, values, dir) error`.
    - `internal/template/template.go` has `RenderToWithPost` that writes files, runs post-hook, and deletes spin.toml.
    - `cmd/new_charm.go` merges ecosystem + template files when `--template` is set.
    - `cmd/new_extras.go` binds `--template` on the legacy v1 `newCmd` and routes through v2 dispatch.
    - `go build ./...` and `go vet ./...` pass.
    - `go test ./internal/template/...` passes (existing tests still green).
  </done>
</task>

<task type="auto">
  <name>Task 3: Registry unit tests + verify `defaultRegistry` is the single source of truth</name>
  <files>internal/ecosystem/registry_test.go, internal/ecosystem/registry.go</files>
  <read_first>
    - internal/ecosystem/registry.go (Get, Names, Detect, RegisterBuiltin)
    - cmd/ecosystem.go (defaultRegistry function)
    - .planning/phases/05-v2-0-universal-scaffolder-task-runner/05-CONTEXT.md (decision A: single registry)
  </read_first>
  <action>
1. Verify `cmd/ecosystem.go` `defaultRegistry()` is the single source of truth — both the new v2 dispatch in `cmd/new.go` AND the existing `spin ecosystem list/info` commands must use the same function. (Plan 01 already updated this to register both charm and rust; this task just confirms the wiring.) Run `grep -rn "ecosystem.NewRegistry" --include="*.go" .` and confirm there is exactly one call site (in `cmd/ecosystem.go`).

2. Add a new file `internal/ecosystem/registry_test.go` with three tests:
   - `TestRegistry_Get_UnknownEcosystem`: create a fresh registry, call `Get("madeup")`, assert the error message contains `"madeup"` and the names of all registered ecosystems.
   - `TestRegistry_Names_StableOrder`: register two ecosystems (any two), assert `Names()` returns them sorted alphabetically.
   - `TestRegistry_Detect_CharmVsRust`: register both charm and rust, create a temp dir with `Cargo.toml`, call `Detect`, assert the rust ecosystem is returned.
   - Use `t.TempDir()` for the temp dir; do not introduce new test dependencies.

3. Run `go test ./internal/ecosystem/...` to confirm green.
  </action>
  <verify>
    <automated>go build ./... && go vet ./... && go test ./internal/ecosystem/... -count=1 -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `internal/ecosystem/registry_test.go` exists with 3 tests, all passing.
    - `go build ./...`, `go vet ./...`, and `go test ./internal/ecosystem/...` all pass.
    - `grep -rn "ecosystem.NewRegistry" --include="*.go" .` shows exactly one call site (in `cmd/ecosystem.go`).
  </done>
</task>

</tasks>

<verification>
- `go build ./...` exits 0
- `go vet ./...` exits 0
- `./spin new myapp --tui --bubbletea --no-git --no-interactive` produces a project tree (deprecation notice printed once)
- The first `spin new <name>` invocation in a process prints the deprecation notice
- `./spin new madeup_eco name --no-interactive` exits non-zero with an error listing known ecosystems
- `./spin new charm demo --no-git --no-interactive` produces a charm project identical to v1
- All v1 commands still work: `./spin build`, `./spin test`, `./spin vet`, `./spin fmt`, `./spin lint`, `./spin doctor`, `./spin update`
- `go test ./internal/ecosystem/...` and `go test ./internal/template/...` pass
</verification>

<success_criteria>
- [ ] `cmd/new.go` prints a one-time deprecation notice and dispatches to the v2 ecosystem flow
- [ ] `cmd/new.go` returns a clear error when given an unknown ecosystem name
- [ ] `cmd/new_charm.go` `runNewCharm` invokes the template loader when `--template` is set
- [ ] `internal/template/loader.go` uses `~/.config/spin/templates/` as cache and sets `GIT_TERMINAL_PROMPT=0`
- [ ] `internal/template/post_hook.go` exists with `RunPostHook`
- [ ] After a `--template` render, `spin.toml` is removed from the output directory
- [ ] Path-traversal safety: any path containing `..` segments is rejected
- [ ] `go build ./...` and `go vet ./...` pass
- [ ] No v1.0 commands regressed
</success_criteria>

<output>
Create `.planning/phases/05-v2-0-universal-scaffolder-task-runner/05-02-SUMMARY.md` when done.
</output>
