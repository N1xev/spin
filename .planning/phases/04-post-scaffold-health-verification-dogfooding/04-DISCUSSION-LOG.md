# Phase 4: Post-Scaffold Health + Verification + Dogfooding - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-06-04
**Phase:** 4-Post-Scaffold Health + Verification + Dogfooding
**Areas discussed:** Doctor, Update

---

## Area Selection

User selected 3 of 4 offered gray areas (Doctor scope & output, Update merge strategy, Add library contract) plus a freeform directive that overrode the Add selection:

> "remove spin add subcommand and the spin update should be only for updating dependencies with old -> newStable -> newLatest format before acepting (with huh)"

This dropped HLTH-02 (spin add) and rewrote HLTH-03 (spin update as dep updater with huh form).

---

## Doctor

| Option | Description | Selected |
|--------|-------------|----------|
| ROADMAP baseline only | 4 checks: Go version, tool presence, charm v2 paths, CGO=0 | ✓ (with #2) |
| Baseline + go.mod hygiene | Also check go.mod Go directive, no v1 imports, Taskfile targets | ✓ |
| Baseline + header coverage | Also verify all generated files have `// generated by spin` header | |

**User's choice:** "1 and 2. generated by will be removed at all!"
**Notes:** The "generated by will be removed" directive dropped HLTH-04 (file headers) from Phase 4 entirely. All generated-file header markers will be stripped.

### Follow-up: scope narrow to universal

| Option | Description | Selected |
|--------|-------------|----------|
| Charm-specific doctor | Validate `charm.land/<lib>/v2` import paths | |
| Universal Go doctor | Drop charm-specific checks; doctor is a general Go health tool | ✓ (implicit) |

**User's choice:** "for a future universal tool i guess doctor should not be for charmbracelet and its imports only or in the scope of doctor at all, this will be stupid thing"
**Notes:** Doctor is a universal Go project health checker, not a charm linter. Charm v2 path check removed entirely.

### Output format

| Option | Description | Selected |
|--------|-------------|----------|
| Both via --format | `human` default (lipgloss), `json` via flag | ✓ |
| Human only | Always lipgloss; CI pipes through grep | |
| JSON only | Always machine-readable | |
| You decide | | |

**User's choice:** "Both via --format"

### Exit code

**User asked for recommendation.** Recommended two-tier + `--strict` (matches `go vet` / `golangci-lint` convention; warnings advisory by default; `--strict` upgrades). User accepted.

### Depth (kitchen-sink vs focused)

| Option | Description | Selected |
|--------|-------------|----------|
| Fast + separate lint cmd | `spin doctor` fast (4 checks), `spin lint` wraps golangci-lint, `spin doctor --deep` includes lint | ✓ |
| Lint baked into doctor | Doctor includes lint unconditionally; slow | |
| Minimal doctor, no lint cmd | 4 checks only; no lint in this phase | |

**User's choice:** "Fast + separate lint cmd"

### Auto-fix

| Option | Description | Selected |
|--------|-------------|----------|
| Read-only + --fix | Default read-only; `--fix` runs safe repairs | ✓ |
| Read-only only | Never modify; user runs `go mod tidy` themselves | |
| Auto-fix in place | Default to auto-fixing; `--no-fix` to disable | |
| You decide | | |

**User's choice:** "Read-only + --fix"

---

## Update

### Project detection (header marker is gone)

| Option | Description | Selected |
|--------|-------------|----------|
| Universal Go tool (no detection) | `spin update` works on any go.mod; same stance as `spin doctor` | ✓ |
| Spin-project only (.spin marker) | Detect via `.spin-version`/`.spin.yaml` written by `spin new` | |
| Heuristic: charm v2 imports | Fragile; false positives | |
| You decide | | |

**User's choice:** "Universal Go tool (no detection)"

### Which deps to check

| Option | Description | Selected |
|--------|-------------|----------|
| Direct require only (default) | Direct deps from `require` block, no indirect | |
| Direct by default, --all for transitive | Opt-in depth via flag | ✓ |
| Direct + indirect always | Always noisy/comprehensive | |
| You decide | | |

**User's choice:** "Direct by default, --all for transitive"

### `newStable` definition

| Option | Description | Selected |
|--------|-------------|----------|
| SemVer suffix (beta/rc/alpha) | `newStable` = no pre-release suffix; `newLatest` = highest semver desc (may include pre) | ✓ |
| Same definition, separate labels | Use proxy's "Latest" + "LatestStable" — not actually exposed | |
| Stable = vN.M.0+ only | Highest minor, ignore beta of next minor | |
| You decide | | |

**User's choice:** "SemVer suffix (beta/rc/alpha)"

### Huh form shape

| Option | Description | Selected |
|--------|-------------|----------|
| Multi-field huh (one form, all deps) | One form, N Select fields, one submit, atomic apply | ✓ (recommended) |
| Per-dep huh confirm loop | Slow for many deps; explicit per-dep control | |
| Table + per-row checkboxes | Loses `newLatest` choice | |
| You decide | | |

**User asked for recommendation.** Recommended Option 1 (multi-field) for: info density (all three columns visible), default = newStable (most common), one submit = atomic apply, 1 keypress per dep, huh v2 native pattern. Per-dep loop is too slow. Checkbox table loses the newLatest choice.

**User accepted recommendation.**

### Apply mechanics

**User asked for recommendation.** Recommended `go get` + `go mod tidy` + `go build ./...` smoke (catches 80% of update failures in <15s; doesn't run user tests; consistent with `spin new` post-scaffold smoke).

**User accepted recommendation.**

---

## Claude's Discretion

- Long-form huh behavior for `spin update` with 20+ deps: rely on huh's built-in scroll; defer pagination/grouping until a real user hits a long form.
- `go list -m -versions` vs direct `proxy.golang.org` HTTP for resolving `newStable`/`newLatest`: research phase should pick the cleaner one.
- `spin lint` command's flag set mirrors `golangci-lint`'s by passing through; no spin-specific flags needed.

## Deferred Ideas

- **HLTH-02 (`spin add <lib>`)** — explicitly dropped by user. If reintroduced later, its own phase.
- **HLTH-04 (file headers as owned-by-spin signal)** — explicitly dropped. All `// generated by spin` and `<!-- AUTOGENERATED -->` markers stripped from generated files.
- **Phase 3-04 "AUTOGENERATED" marker on AGENTS.md** — stripped alongside the rest.
- **Spin-config file (`.spin.yaml`, `.spin-version`)** — not needed; `spin update` and `spin doctor` are universal.
- **Project marker detection** — not needed for the same reason.
- **Original HLTH-03 (template merge for `spin update`)** — superseded by D-06 through D-10 (universal Go dep updater).
