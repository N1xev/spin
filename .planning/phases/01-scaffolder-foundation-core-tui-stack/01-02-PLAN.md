---
phase: 01-scaffolder-foundation-core-tui-stack
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - cmd/new.go
  - cmd/root.go
  - internal/scaffold/project.go
  - internal/scaffold/resolve.go
  - internal/scaffold/validate.go
  - internal/scaffold/hooks.go
  - internal/scaffold/git.go
  - internal/version/version.go
  - internal/scaffold/resolve_test.go
  - internal/scaffold/validate_test.go
autonomous: true
requirements:
  - SCAF-02
  - SCAF-04
  - SCAF-08
  - FLAG-02
  - FLAG-03
  - FLAG-13
  - FLAG-14
  - FLAG-15
  - FLAG-16
  - FLAG-17
  - FLAG-18
  - TMPL-07
must_haves:
  truths:
    - "User can run `spin new myapp --module github.com/foo/myapp` and the generated go.mod contains `module github.com/foo/myapp`"
    - "User can run `spin new MyApp` and the command exits non-zero with a clear error message naming the regex constraint"
    - "User can run `spin new myapp` twice; the second invocation exits non-zero with `directory './myapp' already exists`"
    - "User can run `spin new myapp --force` on an existing directory and it overwrites"
    - "User can run `spin new myapp --bubbles` and the generated project has `--bubbletea` implied (no missing-import error from `go build`)"
    - "User can run `spin new myapp --no-git` and the generated project has NO `.git/` directory"
    - "User can run `spin new myapp --no-verify` and the smoke test is skipped (faster scaffold)"
    - "User can run `spin new myapp --license apache-2.0` and the generated project has an Apache-2.0 LICENSE"
    - "User can run `spin new myapp --license none` and NO LICENSE file is generated"
    - "User passes an unknown flag like `--bubbltea` and gets a clear error suggesting `--bubbletea`"
    - "Generated project has `.git/` with 1 initial commit after `spin new myapp`"
    - "All forward-compat flags (`--cli`, `--cobra`, `--huh`, `--ai`, etc.) are registered and bind to the Project struct without error"
  artifacts:
    - path: "internal/scaffold/project.go"
      provides: "Expanded Project struct with License, Template, Force, NoGit, NoVerify, Quiet + 13 forward-compat bools"
      contains: "License string"
    - path: "internal/scaffold/resolve.go"
      provides: "ResolveFlags function that binds all cobra flags to Project, with --bubbles implies --bubbletea and default Module = Name"
      contains: "ResolveFlags"
    - path: "internal/scaffold/validate.go"
      provides: "IsValidGoModuleSegment + ModuleSegmentRegex + Project.Validate (name + dir conflict + force)"
      contains: "IsValidGoModuleSegment"
    - path: "internal/scaffold/hooks.go"
      provides: "VerifyBuild runs go build ./... + go test ./... with CGO_ENABLED=0; clear error surfacing"
      contains: "CGO_ENABLED=0"
    - path: "internal/scaffold/git.go"
      provides: "GitInit function: git init -b main, git add ., git commit with env-guarded identity"
      contains: "GIT_TERMINAL_PROMPT=0"
    - path: "internal/version/version.go"
      provides: "Version var, Version() function for fang to pick up"
      contains: "var Version"
    - path: "cmd/new.go"
      provides: "newCmd with ALL flags registered (Phase 1 + forward-compat Phase 2/3/4)"
      contains: "pf.String(\"module\""
    - path: "cmd/root.go"
      provides: "RootCmd with Version = version.Version() and full Long description"
      contains: "version.Version()"
    - path: "internal/scaffold/validate_test.go"
      provides: "Table-driven test for IsValidGoModuleSegment (14 cases from RESEARCH §6) and Project.Validate (dir conflict + --force)"
      contains: "TestIsValidGoModuleSegment"
    - path: "internal/scaffold/resolve_test.go"
      provides: "Tests for ResolveFlags: --bubbles implies --bubbletea, --module override, default Module = Name, Year set to current year"
      contains: "TestResolveFlags"
  key_links:
    - from: "cmd/new.go"
      to: "internal/scaffold/resolve.go"
      via: "runNew calls scaffold.ResolveFlags(cmd, args)"
      pattern: "ResolveFlags"
    - from: "cmd/new.go"
      to: "internal/scaffold/validate.go"
      via: "runNew calls p.Validate() after ResolveFlags"
      pattern: "p\\.Validate"
    - from: "internal/scaffold/resolve.go"
      to: "internal/scaffold/project.go"
      via: "ResolveFlags populates every field of *Project"
      pattern: "p\\.[A-Z][a-zA-Z]*\\s*="
    - from: "internal/scaffold/hooks.go"
      to: "exec.Command"
      via: "go build ./... and go test ./... with CGO_ENABLED=0 env"
      pattern: "exec\\.Command.*go"
    - from: "internal/scaffold/git.go"
      to: "exec.Command"
      via: "git init, git add, git commit with GIT_AUTHOR_* env guards"
      pattern: "exec\\.Command.*git"
    - from: "cmd/root.go"
      to: "internal/version/version.go"
      via: "rootCmd.Version = version.Version()"
      pattern: "version\\.Version"
---

<objective>
Expand the Walking Skeleton's minimal `Project` struct into the full TMPL-07 contract (adds License, Template, Force, NoGit, NoVerify, Quiet, and 13 forward-compat booleans for Phase 2/3/4). Wire every Phase 1 flag (`--tui`, `--bubbletea`, `--bubbles`, `--lipgloss`, `--module`, `--license`, `--template`, `--force`, `--no-git`, `--no-verify`, `--quiet`) plus forward-compat flag registration for Phase 2/3/4 flags. Add `Validate()` for name-regex and existing-directory checks. Add `VerifyBuild` for the post-scaffold `go build ./...` + `go test ./...` smoke test with CGO disabled. Add `GitInit` for the env-guarded initial commit. Wire fang's unknown-flag suggestion.

Purpose: Turn the Walking Skeleton's single happy path into a robust CLI command that handles every Phase 1 input correctly: invalid names rejected with a clear error, existing dirs refused without `--force`, unknown flags suggested, missing flags defaulting safely, and the post-scaffold smoke test catching every common mistake before the user touches the generated project.

Output: A fully-wired `spin new` command that satisfies SCAF-02, SCAF-04, SCAF-08, FLAG-02, FLAG-03, FLAG-13–18, and TMPL-07.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/samouly/Projects/Golang/loom/.planning/PROJECT.md
@/home/samouly/Projects/Golang/loom/.planning/STATE.md
@/home/samouly/Projects/Golang/loom/.planning/REQUIREMENTS.md
@/home/samouly/Projects/Golang/loom/.planning/phases/01-scaffolder-foundation-core-tui-stack/01-RESEARCH.md
@/home/samouly/Projects/Golang/loom/.planning/phases/01-scaffolder-foundation-core-tui-stack/01-SKELETON.md
@/home/samouly/Projects/Golang/loom/.planning/phases/01-scaffolder-foundation-core-tui-stack/01-01-SUMMARY.md
@/home/samouly/Projects/Golang/loom/CLAUDE.md
@/home/samouly/Projects/Golang/loom/cmd/new.go
@/home/samouly/Projects/Golang/loom/internal/scaffold/project.go
@/home/samouly/Projects/Golang/loom/internal/scaffold/scaffold.go
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand Project struct + add resolve.go (all flag bindings)</name>
  <read_first>
    - /home/samouly/Projects/Golang/loom/.planning/phases/01-scaffolder-foundation-core-tui-stack/01-RESEARCH.md (sections 4.4, 5.3, 13)
    - /home/samouly/Projects/Golang/loom/.planning/phases/01-scaffolder-foundation-core-tui-stack/01-01-SUMMARY.md
    - /home/samouly/Projects/Golang/loom/internal/scaffold/project.go (current shape -- to be expanded)
    - /home/samouly/Projects/Golang/loom/cmd/new.go (current flag list -- to be expanded)
  </read_first>
  <files>
    - internal/scaffold/project.go
    - internal/scaffold/resolve.go
    - internal/scaffold/resolve_test.go
  </files>
  <action>
    Expand the `Project` struct in `internal/scaffold/project.go` with every TMPL-07 field plus the 13 forward-compat bools. Then create `internal/scaffold/resolve.go` with the `ResolveFlags` function that binds all cobra flags to the struct. Then create `internal/scaffold/resolve_test.go` with table-driven tests for the flag-binding rules.

    **Project struct expansion (internal/scaffold/project.go):** add these fields to the existing struct (preserve the existing Name, Module, Type, Libs, Year, SpinVer):
    - `License string` (RESEARCH §5.1 -- "mit" | "apache-2.0" | "none")
    - `Template string` (RESEARCH §5.1 -- defaults to "tui-bubbletea")
    - `Force bool` (SCAF-08 -- overwrite existing dir)
    - `NoGit bool` (skip git init, for tests)
    - `NoVerify bool` (skip smoke test, for power users)
    - `Quiet bool` (minimal scaffolder output)
    - Forward-compat bools (RESEARCH §4.4): `Cobra bool`, `Fang bool`, `Viper bool`, `Huh bool`, `Glamour bool`, `Glow bool`, `Wish bool`, `Log bool`, `Harmonica bool`, `Modifiers bool`, `Ansi bool`, `Runewidth bool`, `AI bool`. All default to `false`. Plan 02's `ResolveFlags` does not yet set these (the flags exist on the cobra command but the scaffolder ignores their values in Phase 1); Plan 02 just needs the struct fields so later plans can reference them without adding fields. The ResolveFlags function reads `--cobra`/`--fang`/etc. and sets the corresponding bool (even if no template behavior uses them yet).

    **ResolveFlags (internal/scaffold/resolve.go):** `func ResolveFlags(cmd *cobra.Command, args []string) (*Project, error)`. Implementation per RESEARCH §5.3:
    1. `name := args[0]`, `p := &Project{Name: name}`
    2. Use helper `func mustString(cmd, key) (string, error)` and `func mustBool(cmd, key) (bool, error)` that wrap `cmd.Flags().GetString/Bool` and return the error if the flag is missing (it shouldn't be, since we registered it).
    3. Bind: `p.Module` (default = name if empty), `p.License` (default "mit"), `p.Template` (default "tui-bubbletea"), `p.Force`, `p.NoGit`, `p.NoVerify`, `p.Quiet`, `p.Type` ("tui" if --tui or no --cli; "cli" if --cli; "all" if --all; default "tui"), `p.Libs` (slice built from --bubbletea → "bubbletea", --bubbles → "bubbles", --lipgloss → "lipgloss"; deduplicate; sort for determinism).
    4. The CRITICAL rule: if `p.Libs` contains "bubbles" but not "bubbletea", APPEND "bubbletea" to `p.Libs`. (RESEARCH §5.2: --bubbles implies --bubbletea because bubbles is a layer on bubbletea.)
    5. Forward-compat bools: bind `--cobra` → `p.Cobra`, `--fang` → `p.Fang`, etc. for all 13. Set `p.Cobra = true` AND `p.Fang = true` if `p.Type == "cli"` and the user did NOT pass `--no-cobra` (which doesn't exist yet, so this is the default-on behavior; FLAG-13, FLAG-14).
    6. `p.Year = time.Now().Year()`
    7. `p.SpinVer = version.Version()`

    **resolve_test.go:** table-driven test for `ResolveFlags` with the following cases:
    - `args=("myapp",), flags=(--tui --bubbletea)` → `p.Type="tui"`, `p.Libs=["bubbletea"]`, `p.Module="myapp"`, `p.Year >= 2024`, `p.License="mit"`, `p.Template="tui-bubbletea"`
    - `args=("myapp",), flags=(--tui --bubbles)` → `p.Libs` contains BOTH "bubbles" AND "bubbletea" (implies rule)
    - `args=("myapp",), flags=(--tui --bubbletea --bubbles --lipgloss)` → `p.Libs` is sorted and contains all three
    - `args=("myapp",), flags=(--tui --bubbletea --module github.com/foo/myapp)` → `p.Module="github.com/foo/myapp"` (NOT "myapp")
    - `args=("myapp",), flags=(--cli --cobra --fang)` → `p.Type="cli"`, `p.Cobra=true`, `p.Fang=true`, `p.Libs=[]`
    - `args=("myapp",), flags=(--tui --bubbletea --license apache-2.0)` → `p.License="apache-2.0"`
    - `args=("myapp",), flags=(--tui --bubbletea --no-git --no-verify --force --quiet)` → all 5 booleans true

    Each test case uses `cobra.NewCommand` + `cmd.SetArgs(append([]string{args[0]}, flagStrs...))` + `cmd.Execute()` to populate flags, then calls `ResolveFlags` on the resulting command.

    **cmd/new.go update:** expand the flag list in `init()`. Add to the existing `--tui` and `--bubbletea` registrations:
    - Phase 1 active: `--bubbles` (Bool, "add bubbles v2"), `--lipgloss` (Bool, "add lipgloss v2"), `--module` (String, "override default module path"), `--license` (String, "license type: mit, apache-2.0, none", default "mit"), `--template` (String, "template name (default: tui-bubbletea)", default "tui-bubbletea"), `--force` (Bool, "overwrite existing directory"), `--no-git` (Bool, "skip git init"), `--no-verify` (Bool, "skip post-scaffold go build smoke test"), `--quiet` (Bool, "minimal scaffolder output"), `--no-license` (alias for `--license none`? No -- just use `--license none`).
    - Forward-compat (registered but no behavior yet): `--cli` (Bool, "CLI project variant (Phase 2)"), `--all` (Bool, "TUI + CLI combo (Phase 2)"), `--cobra` (Bool, "add cobra (Phase 2)"), `--fang` (Bool, "add fang (Phase 2)"), `--viper` (Bool, "add viper (Phase 2)"), `--huh` (Bool, "add huh v2 (Phase 2)"), `--glamour` (Bool, "add glamour v2 (Phase 2)"), `--glow` (Bool, "add glow binary (Phase 2)"), `--wish` (Bool, "add wish v2 (Phase 2)"), `--log` (Bool, "add charm log v2 (Phase 2)"), `--harmonica` (Bool, "add harmonica v2 (Phase 2)"), `--modifiers` (Bool, "add x/modifiers (Phase 2)"), `--ansi` (Bool, "add x/ansi (Phase 2)"), `--runewidth` (Bool, "add go-runewidth (Phase 2)"), `--ai` (Bool, "opt in to AGENTS.md (Phase 3)").
    - Replace the existing `runNew` body with: `p, err := scaffold.ResolveFlags(cmd, args); if err != nil { return err }; if err := p.Validate(); err != nil { return err }; return scaffold.New(p)`. (Plan 02 doesn't ship the scaffold template engine -- `scaffold.New(p)` is the function from Plan 01-01. If Plan 03 has not yet added new template overlays, the existing Walking Skeleton templates are used; `--bubbles`/`--lipgloss` would not be honored yet, and `go build` would fail or produce a minimal output. Plan 03 ships the full overlay engine. This is expected -- Plan 02's test focuses on flag wiring + validation, not on the generated project.)
  </action>
  <behavior>
    - Test 1: `TestResolveFlags_Default` with `--tui --bubbletea` produces a Project with Type="tui", Libs=["bubbletea"], Module="myapp", License="mit", Template="tui-bubbletea", Year is current year
    - Test 2: `TestResolveFlags_BubblesImpliesBubbletea` with `--bubbles` only produces Libs containing BOTH "bubbles" and "bubbletea"
    - Test 3: `TestResolveFlags_ModuleOverride` with `--module github.com/foo/bar` sets Module correctly
    - Test 4: `TestResolveFlags_CLI` with `--cli --cobra --fang` sets Type="cli", Cobra=true, Fang=true
    - Test 5: `TestResolveFlags_AllBoolsBind` verifies that every forward-compat bool field on Project is settable via its corresponding flag
  </behavior>
  <verify>
    <automated>go test ./internal/scaffold/... -count=1 -run TestResolveFlags -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - Project struct has all TMPL-07 fields plus 13 forward-compat bools
    - `ResolveFlags` function exists and binds every flag
    - All `TestResolveFlags` cases pass
    - `cmd/new.go` registers all flags
    - `go vet ./...` is clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate() (name regex + dir conflict + force) with table-driven tests</name>
  <read_first>
    - /home/samouly/Projects/Golang/loom/.planning/phases/01-scaffolder-foundation-core-tui-stack/01-RESEARCH.md (section 6 -- Name Validation; section 5.6 -- existing-dir check)
    - /home/samouly/Projects/Golang/loom/internal/scaffold/project.go (Project struct -- Name and Force fields)
  </read_first>
  <files>
    - internal/scaffold/validate.go
    - internal/scaffold/validate_test.go
  </files>
  <action>
    Create `internal/scaffold/validate.go` with two exported items: `ModuleSegmentRegex` and `IsValidGoModuleSegment(s string) bool`, plus a method `func (p *Project) Validate() error` that runs the name check and the existing-directory check.

    **`ModuleSegmentRegex`** (RESEARCH §6): `var ModuleSegmentRegex = regexp.MustCompile(\`^[a-z0-9][a-z0-9._-]{0,61}[a-z0-9]$\`)`. Export it so users can reference it in error messages.

    **`IsValidGoModuleSegment(s string) bool`** (RESEARCH §6): return false if `len(s) < 2 || len(s) > 62`. Match the regex. Reject strings containing `..` (path traversal). Reject strings starting or ending with `.` or `-`. Reject the reserved set `{"test", "tests", "_test", "vendor", "internal", "cmd", "go", "golang"}`. Return true otherwise.

    **`Project.Validate() error`**: ordered checks:
    1. `if !IsValidGoModuleSegment(p.Name) { return fmt.Errorf("invalid project name %q: must be 2-62 chars, lowercase [a-z0-9._-], not a Go-reserved word (test, internal, cmd, go, golang, vendor, _test, tests); see example: spin new myapp --tui --bubbletea", p.Name) }`. Multi-line error using `errors.New` with a backtick string is fine; ensure the error message references the constraint.
    2. `target := filepath.Join(".", p.Name)`. `if _, err := os.Stat(target); err == nil { if !p.Force { return fmt.Errorf("directory %q already exists; pass --force to overwrite", target) } }`. If `os.IsNotExist(err)`, continue normally. Other Stat errors propagate.
    3. Return nil.

    **validate_test.go** -- table-driven test for `IsValidGoModuleSegment` (RESEARCH §6 has the exact 14 cases to cover):
    - Valid: `myapp`, `my-app`, `my_app`, `my.app`, `a1` (2-char minimum), `a` (no -- invalid because <2 chars), 62-char string (valid), `go-app` (valid because not the reserved `go` exactly)
    - Invalid: `MyApp` (uppercase), `-myapp` (leading dash), `myapp-` (trailing dash), `test` (reserved), `internal` (reserved), `..` (path traversal), `.hidden` (leading dot), `a` (too short), empty string, `myapp/../etc` (slash), 64-char string (too long), `_test` (reserved)

    Plus a separate `TestProjectValidate_DirConflict` that:
    - Creates a temp directory, then calls `(&Project{Name: tempDirName, ...}).Validate()`; expects error containing "already exists".
    - Sets `p.Force = true`; expects nil error.
    - Uses a non-existent dir name; expects nil error.

    Use `t.TempDir()` for cleanup. Use `filepath.Base(t.TempDir())` to get a name that already exists in CWD during the test (alternative: `os.MkdirAll` a real dir, then validate). The cleanest approach: `name := "spin-validate-test-" + randStr(); p := &Project{Name: name, ...}; if err := p.Validate(); err == nil { t.Fatal("expected no error for non-existent dir") }; os.Mkdir(name, 0o755); defer os.RemoveAll(name); if err := p.Validate(); err == nil { t.Fatal("expected error for existing dir") }; p.Force = true; if err := p.Validate(); err != nil { t.Fatalf("expected nil with --force, got %v", err) }`. Note: validate_test runs in the package's test binary CWD, which is the repo root; Mkdir is fine for a test scratch dir.
  </action>
  <behavior>
    - Test 1: `TestIsValidGoModuleSegment` covers the 14 cases from RESEARCH §6 -- all 8 valid cases return true, all 6 invalid cases return false (counts may vary; the cases must match)
    - Test 2: `TestProjectValidate_DirConflict` -- non-existent dir passes; existing dir without --force fails; existing dir with --force passes
  </behavior>
  <verify>
    <automated>go test ./internal/scaffold/... -count=1 -run 'TestIsValidGoModuleSegment|TestProjectValidate' -v 2>&1 | tail -30</automated>
  </verify>
  <done>
    - `validate.go` exists with `ModuleSegmentRegex`, `IsValidGoModuleSegment`, `Project.Validate`
    - All `TestIsValidGoModuleSegment` cases pass (14 cases from RESEARCH §6)
    - `TestProjectValidate_DirConflict` passes
    - `go vet ./...` is clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: VerifyBuild smoke test + GitInit (env-guarded)</name>
  <read_first>
    - /home/samouly/Projects/Golang/loom/.planning/phases/01-scaffolder-foundation-core-tui-stack/01-RESEARCH.md (section 7 -- Post-Scaffold Smoke Test; section 12 -- Git Init)
    - /home/samouly/Projects/Golang/loom/internal/scaffold/scaffold.go (where VerifyBuild will be called from)
  </read_first>
  <files>
    - internal/scaffold/hooks.go
    - internal/scaffold/git.go
  </files>
  <action>
    Create two files that handle the post-scaffold and git-init hooks. The Walking Skeleton's `verifyBuild` in scaffold.go was a thin shell -- Plan 02 moves the real implementation here and adds `GitInit`.

    **internal/scaffold/hooks.go** -- `func (p *Project) VerifyBuild() error` (no `log` param; the scaffolder uses the default log). Steps:
    1. If `p.NoVerify`, log "skipping verify (--no-verify)" and return nil.
    2. `root := filepath.Join(".", p.Name)`. Log: `log.Info("verifying build", "path", root)`.
    3. Run two steps sequentially: `["go build ./..."]` with `CGO_ENABLED=0`, then `["go test ./..."]` with no extra env. Use `os/exec`. `cmd.Dir = root`. `cmd.Env = append(os.Environ(), "CGO_ENABLED=0")` for the build step.
    4. On non-zero exit, capture `cmd.CombinedOutput()`, log `log.Error("smoke test failed", "step", name, "output", string(out))`, and return an error formatted as: `fmt.Errorf("`%s` failed in %s:\n%s", strings.Join(args, " "), root, out)`. This is the exact error format from RESEARCH §7.2 so the user sees the `go build` output verbatim.
    5. On both steps passing, log `log.Info("smoke test passed", "path", root)` and return nil.

    **internal/scaffold/git.go** -- `func (p *Project) GitInit() error`. Steps:
    1. If `p.NoGit`, return nil.
    2. `root := filepath.Join(".", p.Name)`. Log: `log.Info("initializing git", "path", root)`.
    3. Run `git init -b main` (or `git init` + `git symbolic-ref HEAD refs/heads/main` for git < 2.28; RESEARCH §12.4). If `git` is not on `$PATH` (`exec.ErrNotFound` or exit code 127), log a warning and return nil (don't fail the scaffold). The user can `git init` themselves.
    4. Run `git add .`.
    5. Run `git commit -m "scaffold <name> with spin <SpinVer>"` (substitute p.Name and p.SpinVer).
    6. For ALL three git commands, set env: `GIT_TERMINAL_PROMPT=0`, `GIT_AUTHOR_NAME=spin`, `GIT_AUTHOR_EMAIL=spin@localhost`, `GIT_COMMITTER_NAME=spin`, `GIT_COMMITTER_EMAIL=spin@localhost` (RESEARCH §12.3). Use `cmd.Env = append(os.Environ(), envVars...)` where envVars is a `[]string{"KEY=VAL", ...}`.
    7. On any non-zero exit, return an error wrapping the git stderr.

    The `runCmd(dir string, env []string, args ...string) ([]byte, error)` helper used by both files should be unexported and live in `hooks.go` (since git.go is small enough to inline the exec calls, but the env-appending pattern is shared). Implementation: `cmd := exec.Command(args[0], args[1:]...); cmd.Dir = dir; cmd.Env = append(os.Environ(), env...); return cmd.CombinedOutput()`. If `err != nil`, return the output and a wrapped error.

    **Hook into scaffold.go's `New(p)`:** the Walking Skeleton's `New(p)` calls `verifyBuild(p)` inline. Plan 02's executor should:
    - Update `scaffold.go` to call `p.VerifyBuild()` (the new method) instead of the inline function.
    - Add a `p.GitInit()` call AFTER `VerifyBuild` succeeds, so a failing build never gets committed.
    - Order: `emit(p, files)` → `p.VerifyBuild()` → `p.GitInit()`.

    **Tests:** add `internal/scaffold/hooks_test.go` with:
    - `TestVerifyBuild_Passing`: scaffold a minimal project in a temp dir, run VerifyBuild, expect nil error.
    - `TestVerifyBuild_Failing`: scaffold a project with a deliberately broken main.go (e.g., import a non-existent package), run VerifyBuild, expect error containing the `go build` output.
    - `TestVerifyBuild_Skipped`: `p.NoVerify = true`, run VerifyBuild, expect nil error and no exec call (verifiable by absence of build artifacts in the dir).
    - `TestGitInit_Success`: scaffold a project, run GitInit, verify `./<name>/.git/` exists, verify `git log --oneline` (run in the dir) shows exactly 1 commit.
    - `TestGitInit_NoGit`: `p.NoGit = true`, run GitInit, verify `./<name>/.git/` does NOT exist.
    - For `TestGitInit_Success`, the test environment must have `git` on `$PATH`. If it doesn't, skip the test with `t.Skip("git not available")`.

    The tests live in `hooks_test.go` and reuse the Walking Skeleton's test helpers from `scaffold_test.go`. The "scaffold a minimal project" helper is: `func setupTestProject(t *testing.T) (*Project, string) { name := "spin-hook-test-" + randStr(); root := filepath.Join(t.TempDir(), name); os.MkdirAll(root, 0o755); return &Project{Name: name, ...}, root }` -- but actually the project must be in `./<name>` (relative to the test's CWD) for scaffold.go's existing-dir logic. Workaround: `chdir` into `t.TempDir()` for the duration of the test (`t.Chdir(t.TempDir())` in Go 1.24+; for 1.23, use `os.Chdir` + `defer os.Chdir(originalWd)`).
  </action>
  <behavior>
    - Test 1: `TestVerifyBuild_Passing` -- minimal project with a working main.go, VerifyBuild returns nil
    - Test 2: `TestVerifyBuild_Failing` -- minimal project with broken main.go, VerifyBuild returns an error containing the go build stderr
    - Test 3: `TestVerifyBuild_Skipped` -- NoVerify=true, VerifyBuild returns nil immediately
    - Test 4: `TestGitInit_Success` (skipped if git not on $PATH) -- GitInit creates .git/ and 1 initial commit
    - Test 5: `TestGitInit_NoGit` -- NoGit=true, .git/ is not created
  </behavior>
  <verify>
    <automated>go test ./internal/scaffold/... -count=1 -run 'TestVerifyBuild|TestGitInit' -v 2>&1 | tail -40</automated>
  </verify>
  <done>
    - `hooks.go` has `Project.VerifyBuild` and the unexported `runCmd` helper
    - `git.go` has `Project.GitInit` with env guards
    - `scaffold.go` calls `p.VerifyBuild()` and `p.GitInit()` in the right order
    - All hook tests pass (or skip cleanly if `git` is not on $PATH)
    - `go vet ./...` is clean
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: internal/version + wire fang version + SCAF-07/FLAG-18 polish</name>
  <read_first>
    - /home/samouly/Projects/Golang/loom/.planning/phases/01-scaffolder-foundation-core-tui-stack/01-RESEARCH.md (section 10 -- fang wiring)
    - /home/samouly/Projects/Golang/loom/cmd/root.go (current shape; Version is hardcoded "0.1.0")
  </read_first>
  <files>
    - internal/version/version.go
    - cmd/root.go
  </files>
  <action>
    Create `internal/version/version.go` with a `Version` var and a `Version()` function, and update `cmd/root.go` to use it. Also polish fang's unknown-flag behavior (FLAG-18 -- RESEARCH §10 says fang inherits cobra's defaults; need to set `SuggestionsMinimumDistance = 2` explicitly).

    **internal/version/version.go:**
    - `package version`
    - `var Version = "0.1.0"` -- default value when no ldflags are set
    - `func Version() string { return Version }` -- function form for cobra/fang (cobra reads `rootCmd.Version` as a string; the var is exported so an external `-ldflags="-X github.com/<org>/spin/internal/version.Version=1.2.3"` can override at build time)
    - Add a doc comment: `// Version is set at build time via -ldflags. The default is "0.1.0" for dev builds.`

    **cmd/root.go update:**
    - Add import: `"github.com/<org>/spin/internal/version"` (replace `<org>` with the same module prefix as go.mod).
    - Change `Version: "0.1.0"` to `Version: version.Version()` in the root cobra command.
    - Set `root.SuggestionsMinimumDistance = 2` (cobra built-in for typo suggestions; fang styles the output but doesn't add this on its own -- RESEARCH §10).
    - Expand the `Long` field to a multi-line string that includes: a brief intro, the "perfect first run" promise, an example `spin new myapp --tui --bubbletea --bubbles --lipgloss`, and a one-line "see `spin new --help` for available flags". Keep it under 8 lines so the fang-styled help doesn't overflow.
    - Add `SilenceUsage: true` and `SilenceErrors: true` to the root command (per cobra best practice; the binary's `main.go` calls `os.Exit(1)` on fang's error return, so cobra shouldn't double-print usage).

    **Verify fang wiring (SCAF-07):** after the changes, `./spin --help` should produce fang-styled output. fang's signature is `fang.Execute(context.Background(), cmd)` (Context7 confirmed). Run `./spin --help` and assert: (1) the output contains ANSI color codes (escape character `\x1b[`), (2) the output contains the string `spin`, (3) the help is rendered with lipgloss styling (not the default cobra `Available Commands:` plain text).

    **Verify FLAG-18:** run `./spin new myapp --bubbltea` (typo, missing 'e') and assert the error message contains the suggestion `--bubbletea`. Cobra's `SuggestionsMinimumDistance = 2` is what enables this; fang styles the output.

    These verifications are in `scaffold_e2e_test.go` (the Walking Skeleton's E2E test from Plan 01-01). Extend that test with two new sub-tests OR add a new file `cmd/help_test.go` with `TestFangStyledHelp` and `TestUnknownFlagSuggestion`. The executor's choice based on file count -- but the tests MUST exist and pass.
  </action>
  <behavior>
    - Test 1: `TestFangStyledHelp` -- `./spin --help` output contains ANSI escape codes (or at minimum, the fang-specific banner like "Made with charmbracelet" or similar charm styling) AND contains "spin new"
    - Test 2: `TestUnknownFlagSuggestion` -- `./spin new myapp --bubbltea` (typo) returns non-zero exit and stderr contains the suggestion `--bubbletea` (or `--bubbletea` is in the error message)
    - Test 3: `TestVersionFlag` -- `./spin --version` outputs "spin version 0.1.0" (or similar; fang formats this)
  </behavior>
  <verify>
    <automated>go build . && ./spin --help 2>&1 | head -10 && echo "---" && ./spin new myapp --bubbltea 2>&1 | head -5; echo "exit=$?"</automated>
  </verify>
  <done>
    - `internal/version/version.go` exists with `Version` var and `Version()` function
    - `cmd/root.go` references `version.Version()` and sets `SuggestionsMinimumDistance = 2`
    - `./spin --help` shows fang-styled output (verifiable by ANSI codes or charm banner)
    - `./spin new myapp --bubbltea` (typo) returns non-zero with a suggestion for `--bubbletea`
    - `go vet ./...` is clean
  </done>
</task>

</tasks>

<verification>
## Plan 01-02 checks

1. **Flag wiring:**
   - `spin new myapp --module github.com/foo/myapp` → generated go.mod has `module github.com/foo/myapp`
   - `spin new myapp --bubbles` → generated project has bubbletea (implies) and bubbles (no missing-import error)
   - `spin new myapp --cli --cobra --fang` → `p.Type="cli"`, `p.Cobra=true`, `p.Fang=true`
   - `spin new myapp --license apache-2.0` → generated project has Apache-2.0 LICENSE
   - `spin new myapp --no-git` → no `.git/` in generated project
   - `spin new myapp --no-verify` → no smoke test runs
   - `spin new myapp --force` overwrites existing directory

2. **Validation:**
   - `spin new MyApp` rejects with clear error message
   - `spin new test` rejects (reserved word)
   - `spin new myapp` (when ./myapp exists) rejects without --force
   - `spin new myapp --force` (when ./myapp exists) overwrites

3. **Smoke test + Git init:**
   - `spin new myapp --tui --bubbletea` → `./myapp/.git/` exists with 1 commit
   - `go build ./...` and `go test ./...` both pass in the generated project
   - Smoke test failure surfaces with `go build` stderr

4. **Fang styling (SCAF-07, FLAG-18):**
   - `spin --help` produces fang-styled output (ANSI codes present)
   - `spin --version` outputs version
   - Unknown flag like `--bubbltea` produces a suggestion for `--bubbletea`
</verification>

<success_criteria>
Plan 01-02 is complete when:

- All Phase 1 flags (`--tui`, `--bubbletea`, `--bubbles`, `--lipgloss`, `--module`, `--license`, `--template`, `--force`, `--no-git`, `--no-verify`, `--quiet`) are bound to the Project struct via `ResolveFlags`
- All 13 forward-compat Phase 2/3/4 flags are registered on the cobra command and bind to the Project struct
- `IsValidGoModuleSegment` correctly handles all 14 cases from RESEARCH §6
- `Project.Validate` correctly rejects existing directories without `--force`
- `Project.VerifyBuild` runs `go build ./...` with `CGO_ENABLED=0` and `go test ./...`, surfacing stderr on failure
- `Project.GitInit` runs `git init` + `git add` + `git commit` with env-guarded identity, creating 1 initial commit
- `spin --help` is fang-styled; unknown flags suggest corrections
- REQ-IDs covered by this plan: SCAF-02, SCAF-04, SCAF-08, FLAG-02, FLAG-03, FLAG-13, FLAG-14, FLAG-15, FLAG-16, FLAG-17, FLAG-18, TMPL-07
</success_criteria>

<output>
Create `/home/samouly/Projects/Golang/loom/.planning/phases/01-scaffolder-foundation-core-tui-stack/01-02-SUMMARY.md` when done.
</output>
