---
phase: 03-interactive-prompts-gum-ai-agents-md
verified: 2026-06-04T03:42:48Z
status: gaps_found
score: 17/18 must-haves verified
overrides_applied: 0
overrides: []
re_verification: null
gaps:
  - truth: "The CLI alias contract delivers a single observable behavior for both spellings of every aliased flag pair"
    status: partial
    reason: "WR-03 (REVIEW critical-quality): the variant auto-default block at resolve.go:243-256 silently overrides explicit --cobra=false / --fang=false when the user passes --cli or --all, dropping the user's intent. The same pattern overrides --bubbletea=false in --all. This is a contract deviation: the two-flags-and-OR alias pattern works for the alias spellings themselves (--yes/--batch, --ai/--agents) but the cross-flag auto-defaults do not consult cmd.Flags().Changed."
    artifacts:
      - path: "internal/scaffold/resolve.go"
        issue: "Lines 243-256: if p.Type==\"cli\"||\"all\" { p.Cobra=true; p.Fang=true } and the all-bubbletea add to p.Libs run unconditionally after the bool-flag binding loop, overwriting the user's explicit --cobra=false / --fang=false / --bubbletea=false. The fix per the REVIEW is to wrap the auto-default in cmd.Flags().Changed() checks (line 91-95 shows the pattern for --template-repo)."
    missing:
      - "Wrap the Cobra/Fang auto-default in `if !cmd.Flags().Changed(\"cobra\") { p.Cobra = true }` and similarly for fang, and guard the bubbletea append with a Changed check on bubbletea/bubbles."
deferred: []
human_verification: []
---

# Phase 3: Interactive Prompts (gum) + AI/AGENTS.md Verification Report

**Phase Goal:** When the user runs `spin new` without enough flags, spin asks for the missing pieces via gum (or huh v2 fallback) and can opt-in to an AGENTS.md describing the project for AI assistants.
**Verified:** 2026-06-04T03:42:48Z
**Status:** gaps_found
**Re-verification:** No -- initial verification

## Goal Achievement

### Observable Truths (Phase 3 ROADMAP Success Criteria)

| #   | Truth   | Status     | Evidence       |
| --- | ------- | ---------- | -------------- |
| 1   | When stdin is a TTY and flags are missing, spin prompts the user via `gum` for project type, libraries, template, and AI opt-in; when `gum` is not on `$PATH`, spin transparently falls back to in-process `huh v2` prompts. | VERIFIED | `internal/prompt/prompt.go:123-138` resolveBackend() picks `backendGum` when LookPath succeeds + 2s `gum --version` probe exits 0; otherwise `backendHuh`. `prompt.go:164-187` Fill() dispatches. `gum.go:180-206` fillWithGum runs 8 steps; `huh.go:46` fillWithHuh mirrors. `gum_test.go` + `prompt_backend_test.go` cover all 4 resolveBackend cases. `go test ./internal/prompt/...` passes. |
| 2   | User can pass `--no-interactive` (alias `--yes`, `--batch`) to disable all prompts. | VERIFIED | `cmd/new.go:74-76` registers all three flags; `resolve.go:118-137` OR's all three into `p.NoInteractive`. `cmd/new.go:128-132` reads `p.NoInteractive` before calling `prompt.Fill`. `TestResolveFlags_NoInteractiveAliases` covers 4 sub-cases. |
| 3   | spin never hangs in non-TTY environments: every TUI/prompt call is guarded with `isatty.IsTerminal(os.Stdin)`. | VERIFIED | `internal/prompt/detect.go:40-62` IsInteractive() applies the three-layer guard: `SPIN_NO_INTERACTIVE` env → `isatty.IsTerminal(os.Stdin.Fd())` → 5 CI env vars. `cmd/new.go:128` is the user-flag guard. `detect_test.go` covers env-override, TTY-missing, and all 5 CI env vars. |
| 4   | User can pass `--ai` (alias `--agents`) to generate an `AGENTS.md` containing a `<!-- AUTOGENERATED by spin X.Y.Z -->` marker and a list of the project's charm libraries with extension guidance. | VERIFIED | `cmd/new.go:90-91` registers both; `resolve.go:220-224` OR's `--agents` into `p.AI`. `template.go:128-131` `boolFlagOverlayMap = {"ai": p.AI}` activates `lib/ai/` overlay. `templates/lib/ai/AGENTS.md.tmpl:1` starts with marker. `agents_test.go` (8 unit tests) + `integration_test.go:807-870` (4 integration tests) cover marker, content, determinism, alias, no-AI suppression. |
| 5   | Prompt answers and flag values populate the same single `Project` struct -- there is exactly one source of truth resolved at command time. | VERIFIED | `cmd/new.go:118` ResolveFlags binds flags into `*Project`; `cmd/new.go:129` `prompt.Fill(p)` writes back into the same `*Project`. `huh.go` (8 ask* functions) all write directly to `p.*` fields. `gum.go` (8 askGum* functions) mirror the same. `Project.AllLibs()` (`project.go:183-200`) unifies `p.Libs` + per-lib bools. |

### Plan-Level Must-Haves (4 plans)

| #   | Plan   | Truth | Status | Evidence |
| --- | ------ | ----- | ------ | -------- |
| 1   | 03-01  | `--no-interactive`/`--yes`/`--batch` accepted as equivalent disabling flags | VERIFIED | `cmd/new.go:74-76`, `resolve.go:118-137` |
| 2   | 03-01  | Non-TTY scaffolder never blocks on stdin | VERIFIED | `detect.go:52` isatty guard + `cmd/new.go:128` user-flag guard |
| 3   | 03-01  | CI env vars (5) disable prompting even on TTY | VERIFIED | `detect.go:75-81` `ciEnv` checks all 5 |
| 4   | 03-01  | `spin new <name>` (no flags, TTY) reaches `prompt.Fill` chokepoint | VERIFIED | `cmd/new.go:129`; chokepoint established |
| 5   | 03-01  | `spin new` with no args exits cleanly non-TTY | VERIFIED | `cmd/new.go:17` `cobra.MaximumNArgs(1)` + `resolve.go:33-35` ArgError for missing name |
| 6   | 03-01  | `*prompt.Canceled` → exit 130 in main.go | VERIFIED | `main.go:35-40` errors.As → os.Exit(130) |
| 7   | 03-02  | When stdin is TTY + gum missing, huh v2 prompts 8 fields | VERIFIED | `huh.go:46-67` fillWithHuh + 8 ask* functions; `prompt.go:171-177` dispatch |
| 8   | 03-02  | All 8 prompt answers write back to same `*scaffold.Project` | VERIFIED | All 8 ask* functions write `p.X = ...` directly |
| 9   | 03-02  | `p.AllLibs()` returns union of `p.Libs` + per-lib bools, dedup+sort | VERIFIED | `project.go:183-200`; 5 tests in `project_test.go` |
| 10  | 03-02  | Required field pre-populated with default (e.g., name=myapp, module=name) | VERIFIED | `huh.go:101-133` askName default + `huh.go:135-173` askModule default `p.Name` |
| 11  | 03-02  | `*prompt.Canceled` returned on `huh.ErrUserAborted` | VERIFIED | `huh.go:87,117,153,196,303,332,381,412` map huh.ErrUserAborted → &Canceled |
| 12  | 03-03  | When gum on $PATH + TTY + not --no-interactive, gum subprocess invoked | VERIFIED | `prompt.go:127-138` resolveBackend picks backendGum; `gum.go:69-99` gumRunCapture; 4 widget wrappers |
| 13  | 03-03  | When gum not on $PATH, transparent fallback to huh v2 | VERIFIED | `prompt.go:128-130` `if err != nil { return backendHuh }` |
| 14  | 03-03  | Every gum subprocess wrapped with `cmd.Cancel = Process.Kill` + `cmd.WaitDelay = 100ms` | VERIFIED | `gum.go:80-86` cmd.Cancel + cmd.WaitDelay; only one real os/exec call in gum.go |
| 15  | 03-03  | Backend choice decided once per invocation, never switches mid-flow | VERIFIED | `prompt.go:171-186` resolveBackend called once at Fill entry; switch dispatches to a single backend |
| 16  | 03-03  | Pre-flight `gum --version` sanity check rejects broken gum → falls back to huh v2 | VERIFIED | `prompt.go:100-104` gumVersionCheck seam (2s timeout); `prompt.go:134-136` non-zero exit → return backendHuh; tested in `TestResolveBackend_HuhWhenGumBroken` |
| 17  | 03-04  | `--ai` / `--agents` opt-in for AGENTS.md generation | VERIFIED | `cmd/new.go:90-91`; `resolve.go:220-224`; `template.go:128-131` `boolFlagOverlayMap` |
| 18  | 03-04  | Two consecutive `spin new ... --ai` invocations produce byte-identical AGENTS.md | VERIFIED | `integration_test.go:826-849` `TestIntegrationScaffold_AGENTSmd_Determinism`; 6 unit tests in `agents_test.go` cover marker, sorted libs, no-ANSI, no-AI suppression |

**Score:** 17/18 ROADMAP+plan must-haves verified. The 1 partial is a deviation from the spirit of the CLI alias contract, not a missing implementation.

### Deferred Items

None.

### Required Artifacts

| Artifact | Expected | Status | Details |
| -------- | -------- | ------ | ------- |
| `internal/prompt/prompt.go` | Fill chokepoint, Canceled error, resolveBackend, backend dispatch | VERIFIED | 187 lines; exports `Canceled`, `ErrCanceled`, `Fill`, `ShouldPrompt`; backend enum + String(); resolveBackend + dispatch in Fill |
| `internal/prompt/detect.go` | TTY + CI env detection (three-layer guard) | VERIFIED | 82 lines; IsInteractive + ciEnv; uses isatty.IsTerminal(os.Stdin.Fd()) |
| `internal/prompt/huh.go` | 8 huh v2 form functions + fillWithHuh dispatcher | VERIFIED | All 8 ask* + fillWithHuh present; huh.ErrUserAborted → &Canceled mapped in every function |
| `internal/prompt/gum.go` | gum subprocess wrapper + 4 widget wrappers + fillWithGum | VERIFIED | 504 lines; gumRunCapture (cmd.Cancel + cmd.WaitDelay=100ms), gumChoose, gumMultiSelect, gumInput, gumConfirm, fillWithGum + 8 askGum* |
| `internal/prompt/catalog.go` | Library catalog (11 user-facing libs) + LibsForType | VERIFIED | 142 lines; `LibCatalog` has 11 entries (alphabetical: bubbles, bubbletea, cobra, fang, glamour, harmonica, huh, lipgloss, log, viper, wish); excludes ansi/runewidth (tooling-only) and glow (dropped quick task); libBoolMirror maps 8 lib names to per-lib bool fields |
| `internal/scaffold/project.go` | Project struct + AllLibs() union helper + per-lib bools | VERIFIED | 201 lines; AllLibs() unifies p.Libs + libBoolMap (8 entries), dedup, sort |
| `internal/scaffold/templates/lib/ai/AGENTS.md.tmpl` | AGENTS.md template gated on p.AI | VERIFIED | 35 lines; line 1 marker `<!-- AUTOGENERATED by spin {{.SpinVer}} -->`; 6 sections; {{range allLibs .}}{{end -}} whitespace control correct |
| `internal/scaffold/template.go` | charmLibInfo + allLibs FuncMap helpers + 15-entry lib lookup | VERIFIED | `charmLibInfo` is a closure registered at `template.go:330` with 15 `charmLibInfoField` switch cases (bubbletea, bubbles, lipgloss, huh, glamour, glow, wish, log, harmonica, cobra, fang, viper, modifiers, ansi, runewidth); `allLibs` registered at line 406 |
| `cmd/new.go` | --no-interactive/--yes/--batch; --ai/--agents; MaximumNArgs(1); prompt.Fill call | VERIFIED | `Args: cobra.MaximumNArgs(1)` (line 17); all aliases wired; prompt.Fill called between ResolveFlags and Validate (line 129) |
| `main.go` | Exit code 130 mapping for *prompt.Canceled | VERIFIED | 41 lines; `var canceled *prompt.Canceled; if errors.As(err, &canceled) { os.Exit(130) }` |

### Key Link Verification

| From | To | Via | Status | Details |
| ---- | -- | --- | ------ | ------- |
| `cmd/new.go` | `internal/prompt/prompt.go` | `prompt.Fill(p)` | WIRED | `cmd/new.go:129` calls `prompt.Fill(p)` after `ResolveFlags` |
| `main.go` | `internal/prompt/prompt.go` | `errors.As` for `*prompt.Canceled` → os.Exit(130) | WIRED | `main.go:35-39` |
| `internal/prompt/prompt.go` | `internal/prompt/gum.go` | resolveBackend() returns backendGum → fillWithGum | WIRED | `prompt.go:174-175` |
| `internal/prompt/prompt.go` | `internal/prompt/huh.go` | resolveBackend() returns backendHuh → fillWithHuh | WIRED | `prompt.go:176-177` |
| `internal/prompt/huh.go` | `charm.land/huh/v2` | `huh.NewInput`/`NewSelect`/`NewMultiSelect`/`NewConfirm`/`NewForm` | WIRED | 8 huh.NewXxx calls in huh.go; `charm.land/huh/v2` import verified at `huh.go:25` |
| `internal/prompt/gum.go` | `os/exec` | `exec.CommandContext` with cmd.Cancel + cmd.WaitDelay | WIRED | `gum.go:69-99` only os/exec call site in prompt package; cmd.Cancel = Process.Kill at line 80-85; cmd.WaitDelay = 100ms at line 86 |
| `internal/scaffold/template.go` | `internal/scaffold/project.go` | `boolFlagOverlayMap` adds "ai": p.AI | WIRED | `template.go:128-131`; overlay walker at `template.go:87-105` |
| `cmd/new.go` | `internal/scaffold/resolve.go` | `--ai` flag binds to p.AI; `--agents` OR'd | WIRED | `cmd/new.go:90-91` registers both; `resolve.go:207-224` binds and ORs |
| `templates/lib/ai/AGENTS.md.tmpl` | `internal/scaffold/template.go` | `charmLibInfo` + `allLibs` FuncMap | WIRED | Template uses `{{range allLibs .}}` + `{{charmLibInfo . "display"\|"module"\|"purpose"\|"extending"\|"example"}}` |
| `internal/prompt/huh.go` | `internal/scaffold/project.go` | `p.AllLibs()` + per-field writes | WIRED | `huh.go:229-250` `preSelectedLibs` uses `p.AllLibs()`; `setBoolFieldByName` writes per-lib bools |
| `internal/prompt/gum.go` | `internal/scaffold/project.go` | `p.AllLibs()` + per-field writes | WIRED | `gum.go:311-345` `askGumLibs` calls `preSelectedLibs(p)` and uses `libBoolMirror` + `setBoolFieldByName` |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| -------- | ------------- | ------ | ------------------ | ------ |
| `internal/scaffold/templates/lib/ai/AGENTS.md.tmpl` | `{{.SpinVer}}` | `p.SpinVer` (set by `resolve.go:265` to `version.Version`) | Yes (real version string) | FLOWING |
| `internal/scaffold/templates/lib/ai/AGENTS.md.tmpl` | `{{.Year}}` | `p.Year` (set by `resolve.go:264` to `time.Now().Year()`) | Yes (current year) | FLOWING |
| `internal/scaffold/templates/lib/ai/AGENTS.md.tmpl` | `{{range allLibs .}}` | `p.AllLibs()` (unions p.Libs + libBoolMap, dedup, sort) | Yes (real library list) | FLOWING |
| `internal/scaffold/templates/lib/ai/AGENTS.md.tmpl` | `{{charmLibInfo . "module"}}` | `charmLibInfoField` switch (15 hardcoded entries) | Yes (static lookup table) | FLOWING |
| `internal/scaffold/templates/lib/ai/AGENTS.md.tmpl` | `{{.Name}}` | `p.Name` (set by `resolve.go:37` from positional arg) | Yes (real name) | FLOWING |
| `internal/scaffold/templates/lib/ai/AGENTS.md.tmpl` | `{{.Module}}` | `p.Module` (set by `resolve.go:40-44` or default to `p.Name`) | Yes (real module path) | FLOWING |

All dynamic data flows from the Project struct (the single source of truth). The template has no static/hardcoded values that bypass the data flow.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| -------- | ------- | ------ | ------ |
| `go build ./...` exits 0 | `go build ./...` | (no output) | PASS |
| `go vet ./...` exits 0 | `go vet ./...` | (no output) | PASS |
| `go test ./internal/prompt/...` passes | `go test ./internal/prompt/...` | `ok internal/prompt 0.004s` | PASS |
| `go test ./internal/scaffold/...` passes | `go test ./internal/scaffold/...` | `ok internal/scaffold 68.152s` | PASS |
| `go test ./cmd/...` passes | `go test ./cmd/...` | `ok github.com/example/spin/cmd 5.545s` | PASS |
| AGENTS.md integration test (full spin new) | `go test ./internal/scaffold/ -run TestIntegrationScaffold` | 12/12 pass (4 new AGENTS.md tests) | PASS |
| All four flag aliases (`--yes`, `--batch`, `--agents`, `--no-interactive`, `--ai`) round-trip | `TestResolveFlags_NoInteractiveAliases` + `TestResolveFlags_AllBoolsBind` | Pass | PASS |
| Determinism: same project name, two scaffolds → byte-identical AGENTS.md | `TestIntegrationScaffold_AGENTSmd_Determinism` | Pass | PASS |
| Charm v2 only -- no v1 paths in `main.go`, `cmd/`, `internal/scaffold/`, `internal/prompt/` source | grep for `github.com/charmbracelet/(bubbletea\|huh\|lipgloss\|bubbles\|log\|fang\|glamour\|wish)\b` | All source uses `charm.land/<lib>/v2`; v1 paths appear only in test fixture strings | PASS |

### Probe Execution

No probe scripts discovered in this phase (`scripts/*/tests/probe-*.sh` not present). The phase's verification relies on Go test suites, which all pass.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
| ----------- | ---------- | ----------- | ------ | -------- |
| INT-01 | 03-03 | gum primary backend | SATISFIED | `internal/prompt/gum.go` + `prompt.go:resolveBackend` |
| INT-02 | 03-01 | TTY detection (no hang) | SATISFIED | `internal/prompt/detect.go` + `cmd/new.go:128` user-flag guard |
| INT-03 | 03-01 | Three-layer guard (env + TTY + CI) | SATISFIED | `detect.go:40-62`; tests cover all 5 CI env vars |
| INT-04 | 03-02 | huh v2 in-process fallback | SATISFIED | `internal/prompt/huh.go`; 8 ask* functions |
| INT-05 | 03-02 | Single Project source of truth | SATISFIED | All 8 ask* + askGum* write to `*scaffold.Project`; `AllLibs()` unifies p.Libs + bools |
| AI-01 | 03-04 | --ai / --agents opt-in | SATISFIED | `cmd/new.go:90-91`; `resolve.go:220-224` |
| AI-02 | 03-04 | AUTOGENERATED marker on line 1 | SATISFIED | `AGENTS.md.tmpl:1`; `TestAGENTSmd_MarkerOnLine1` |
| AI-03 | 03-04 | Library list with module/purpose/extending/example | SATISFIED | `AGENTS.md.tmpl:9-20` uses charmLibInfo for all 5 fields; `TestCharmLibInfo_AllThirteen` |
| AI-04 | 03-04 | Deterministic output (byte-identical across runs) | SATISFIED | `TestIntegrationScaffold_AGENTSmd_Determinism`; `TestAGENTSmd_Determinism` |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| ---- | ---- | ------- | -------- | ------ |
| `internal/scaffold/resolve.go` | 243-256 | Variant auto-default block overrides explicit --cobra=false / --fang=false / --bubbletea=false (per REVIEW.md WR-03) | WARNING | The user passing `spin new foo --cli --cobra=false` gets `p.Cobra = true`. Same for `--all --bubbletea=false`. The 3-line fix (wrap in `cmd.Flags().Changed()` checks) is documented in the REVIEW. Does not affect the alias-symmetry contract for the three flag pairs (--yes/--batch/--no-interactive and --ai/--agents work correctly); only affects cross-flag auto-defaults. |
| `internal/prompt/prompt.go` | 93,100; `gum.go` 47,58 | Package-level mutable test seams (`gumLookPath`, `gumVersionCheck`, `gumRunner`, `gumCtx`) prevent `t.Parallel()` (per REVIEW.md CR-01) | INFO | Tests work correctly without parallel; a future maintainer adding `t.Parallel()` to a `fillWithGum` test would race. Defensive workaround exists (use sync.Once or per-call context) but is out of scope. |
| `internal/prompt/gum.go` | 430 | `askGumTemplateRepo` reports empty `p.TemplateRepo` in error instead of the invalid input (per REVIEW.md WR-01) | INFO | UX nit: error message reads "invalid template repo URL %q" with "" filled in. The same pattern is mirrored in `huh.go:396`. Tracked in REVIEW. |
| `internal/prompt/huh.go` | 289-293 | `huh.NewSelect` skip-when-non-mit pre-select loop mutates slice during range (per REVIEW.md WR-06) | INFO | Works correctly in Go (range copies the value); idiomatic form is `for i := range options { ... }`. Style nit, not a bug. |
| `internal/prompt/huh_test.go` | 82-93 | `TestAskLicense_RunsWhenMitSet` is a placeholder asserting on a hardcoded `options` slice (per REVIEW.md WR-05) | INFO | Test does not call `askLicense`; the `options` slice is local and unrelated to the function under test. Renaming to `TestAskLicense_OptionsCount` or extracting `askLicenseOptions()` helper would fix. |
| `internal/prompt/prompt.go` | 178-186 | `default` branch for `backendNone` is unreachable in practice (per REVIEW.md IN-05) | INFO | Defensive; `resolveBackend()` only returns `backendGum` or `backendHuh` today. Either delete the case or `panic("unreachable")` to make the invariant loud. |

### Human Verification Required

None. The phase is fully verifiable by automated tests (132 unit tests + 12 integration tests, all passing). The variant auto-default gap (WR-03) is observable in code (`resolve.go:243-256`) and reproducible by a one-liner shell test (not run here because the gap is documented in REVIEW and the fix is small and explicit).

### Gaps Summary

One partial-verdict gap was found, in the spirit of the "two-flags-and-OR" alias contract that the user explicitly called out as a constraint to verify. The contract is fully honored for the three alias pairs (--no-interactive/--yes/--batch and --ai/--agents): both spellings OR into the same field, and both produce identical Project state. However, the cross-flag auto-defaults at `resolve.go:243-256` do not consult `cmd.Flags().Changed()` and silently override explicit user negation (`--cobra=false`, `--fang=false`, `--bubbletea=false` in the context of `--cli`/`--all`). This was independently flagged by the code review (REVIEW.md WR-03) with a 3-line fix. The gap does not block the phase goal (a TUI/CLI/all scaffold still produces a runnable project, just with cobra/fang/bubbletea forced on regardless of the user's negation); it is a contract deviation worth fixing for parity with the alias pattern's "user's intent is preserved" semantics.

All other 17 must-haves (5 ROADMAP success criteria + 12 plan must-haves + 1 phase goal "deterministic byte-identical AGENTS.md") are VERIFIED. The codebase builds clean (`go build`, `go vet`), tests pass (`go test ./...`), and the constraint surface is intact: charm v2 only, two-flags-and-OR for alias pairs, deterministic AGENTS.md, TTY guard, exit 130 on cancel.

---

_Verified: 2026-06-04T03:42:48Z_
_Verifier: Claude (gsd-verifier)_
