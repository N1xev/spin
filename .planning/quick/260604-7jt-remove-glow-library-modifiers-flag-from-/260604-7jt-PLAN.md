---
phase: 260604-7jt-remove-glow-library-modifiers-flag-from-
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - cmd/new.go
  - internal/scaffold/project.go
  - internal/scaffold/template.go
  - internal/scaffold/resolve.go
  - internal/scaffold/versions.go
  - internal/scaffold/resolve_test.go
  - internal/scaffold/project_test.go
  - internal/scaffold/agents_test.go
  - internal/scaffold/integration_test.go
  - internal/scaffold/grep_test.go
  - internal/prompt/catalog.go
  - internal/prompt/huh.go
  - internal/prompt/huh_test.go
  - internal/scaffold/templates/_base/README.md.tmpl
  - internal/scaffold/templates/variant_cli/internal/cmd/root.go.tmpl
  - internal/scaffold/templates/variant_all/internal/cmd/root.go.tmpl
  - internal/scaffold/templates/variant_cli/internal/cmd/readme.go.tmpl
  - internal/scaffold/templates/variant_all/internal/cmd/readme.go.tmpl
  - internal/scaffold/templates/lib/ai/AGENTS.md.tmpl
  - internal/scaffold/templates/lib/glow/README.glow.md.tmpl
autonomous: true
requirements: []
must_haves:
  truths:
    - "`--glow` and `--modifiers` flags no longer appear in `spin new --help`"
    - "Project.Glow and Project.Modifiers fields are removed; the type compiles and all existing tests pass"
    - "charmLibInfo returns non-empty strings for the 13 remaining library keys (bubbletea, bubbles, lipgloss, huh, glamour, wish, log, harmonica, cobra, fang, viper, ansi, runewidth)"
    - "The `readme` subcommand is no longer registered in any variant (variant_cli, variant_all); the readme.go.tmpl files are deleted"
    - "lib/glow/ template overlay is removed; the overlay walker only walks lib/ai/ when --ai is set"
    - "All tests under ./internal/scaffold/..., ./internal/prompt/..., ./cmd/... pass after the change"
  artifacts:
    - path: "internal/scaffold/project.go"
      provides: "Project struct without Glow/Modifiers; libBoolMap drops glow; AllLibs no longer mentions glow"
      contains: "// AllLibs"
    - path: "internal/scaffold/template.go"
      provides: "charmLibInfo with 13 keys; charmPin without glow; boolFlagOverlayMap with only ai; funcMap without hasGlow"
      contains: "case \"ansi\":"
    - path: "internal/scaffold/resolve.go"
      provides: "Bool-flag binding loop without glow or modifiers"
      contains: "{\"wish\", &p.Wish}"
    - path: "internal/scaffold/versions.go"
      provides: "LegacyCharmPins without Glow"
      contains: "Harmonica string"
    - path: "cmd/new.go"
      provides: "Flag registration without --glow or --modifiers"
      contains: "pf.Bool(\"harmonica\""
    - path: "internal/prompt/catalog.go"
      provides: "LibCatalog with 11 entries (no glow, no modifiers); libBoolMirror without glow"
      contains: "\"harmonica\", Display:"
    - path: "internal/prompt/huh.go"
      provides: "setBoolFieldByName without Glow case"
      contains: "case \"Harmonica\":"
    - path: "internal/scaffold/templates/variant_cli/internal/cmd/root.go.tmpl"
      provides: "root.go without readme registration"
      contains: "if hasWish ."
    - path: "internal/scaffold/templates/variant_all/internal/cmd/root.go.tmpl"
      provides: "root.go without readme registration"
      contains: "if hasWish ."
    - path: "internal/scaffold/templates/_base/README.md.tmpl"
      provides: "README without the glow preview bullet"
      contains: "## Project layout"
  key_links:
    - from: "cmd/new.go"
      to: "internal/scaffold/resolve.go"
      via: "removed flags no longer registered, so resolve loop must drop them"
      pattern: "harmonica|ansi|runewidth"
    - from: "internal/scaffold/template.go"
      to: "internal/scaffold/project.go"
      via: "charmLibInfo table is a fixed 13-key lookup; template uses it for AGENTS.md"
      pattern: "case \"(ansi|runewidth|cobra|fang|viper|wish|log|harmonica)\":"
    - from: "internal/prompt/catalog.go"
      to: "internal/scaffold/project.go"
      via: "libBoolMirror maps each LibCatalog name to the Project bool field; both must drop glow"
      pattern: "\"glow\"|\"Glow\"|Glow"
---

## One-line Summary

Drop the `--glow` and `--modifiers` flags, their Project fields, the `lib/glow/` overlay, the `readme` subcommand, and every related test, comment, and catalog entry. Glamour remains the in-process markdown renderer (still wired through `--glamour`); the only motivation for the readme subcommand was the glow shell-out path, so the subcommand is removed with it.

<objective>
Remove the redundant `--glow` flag (glamour already provides in-process markdown rendering, so the glow binary is unnecessary scaffolding) and the non-existent `--modifiers` flag (`charm.land/x/modifiers` is not a real charmbracelet package). This is a refactor: nothing in the generated user projects should change shape beyond losing a flag the user never needed.

Purpose: shrink the CLI surface to libraries that actually exist and actually integrate, which simplifies onboarding, the AGENTS.md library table (15 keys → 13), the multi-select catalog, and the flag-resolution loop.

Output: a tree where `--glow` and `--modifiers` are gone, `Project.Glow` and `Project.Modifiers` are gone, the readme subcommand is gone, lib/glow/ is gone, and all tests still pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@/home/samouly/Projects/Golang/loom/CLAUDE.md

# Source files to edit
@/home/samouly/Projects/Golang/loom/cmd/new.go
@/home/samouly/Projects/Golang/loom/internal/scaffold/project.go
@/home/samouly/Projects/Golang/loom/internal/scaffold/template.go
@/home/samouly/Projects/Golang/loom/internal/scaffold/resolve.go
@/home/samouly/Projects/Golang/loom/internal/scaffold/versions.go
@/home/samouly/Projects/Golang/loom/internal/prompt/catalog.go
@/home/samouly/Projects/Golang/loom/internal/prompt/huh.go
@/home/samouly/Projects/Golang/loom/internal/scaffold/templates/_base/README.md.tmpl
@/home/samouly/Projects/Golang/loom/internal/scaffold/templates/variant_cli/internal/cmd/root.go.tmpl
@/home/samouly/Projects/Golang/loom/internal/scaffold/templates/variant_all/internal/cmd/root.go.tmpl
@/home/samouly/Projects/Golang/loom/internal/scaffold/templates/variant_cli/internal/cmd/readme.go.tmpl
@/home/samouly/Projects/Golang/loom/internal/scaffold/templates/variant_all/internal/cmd/readme.go.tmpl
@/home/samouly/Projects/Golang/loom/internal/scaffold/templates/lib/ai/AGENTS.md.tmpl

# Test files that reference glow/modifiers and need edits
@/home/samouly/Projects/Golang/loom/internal/scaffold/resolve_test.go
@/home/samouly/Projects/Golang/loom/internal/scaffold/project_test.go
@/home/samouly/Projects/Golang/loom/internal/scaffold/agents_test.go
@/home/samouly/Projects/Golang/loom/internal/scaffold/integration_test.go
@/home/samouly/Projects/Golang/loom/internal/scaffold/grep_test.go
@/home/samouly/Projects/Golang/loom/internal/prompt/huh_test.go
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove --glow/--modifiers from source code (struct, flags, lookup tables, catalog, comments)</name>
  <files>cmd/new.go, internal/scaffold/project.go, internal/scaffold/template.go, internal/scaffold/resolve.go, internal/scaffold/versions.go, internal/prompt/catalog.go, internal/prompt/huh.go, internal/prompt/huh_test.go, internal/scaffold/resolve_test.go, internal/scaffold/project_test.go, internal/scaffold/agents_test.go</files>
  <action>
Make the following edits in this exact order (do NOT skip the comment/section-header touch-ups — they keep the source honest about the new state):

1. `cmd/new.go`:
   - Delete the two `pf.Bool("glow", ...)` and `pf.Bool("modifiers", ...)` registrations.
   - In the `// Library flags (...)` comment block, remove `--glow` and `--modifiers` from the listed names. The line becomes: `// --viper, --huh, --glamour, --wish, --log, --harmonica)`.

2. `internal/scaffold/project.go`:
   - In the package comment, drop `Glow` and `Modifiers` from the listed forward-compat booleans. The list becomes: `Cobra, Fang, Viper, Huh, Glamour, Wish, Log, Harmonica, Ansi, Runewidth, AI`.
   - Delete the `Glow bool` and `Modifiers bool` struct fields and their surrounding comment lines.
   - Update the `libBoolMap` function: drop the `"glow"` and `"harmonica"` comment block is still present but the map loses the `"glow": p.Glow` and `"harmonica": p.Harmonica` lines? No — keep harmonica. Only drop the glow entry. The map shrinks to 8 entries.
   - In the `AllLibs` doc comment, drop the mention of glow: `(Cobra, Fang, Viper, Huh, Glamour, Wish, Log, Harmonica)`.
   - In the `libBoolMap` doc comment, drop the parenthetical "(just 'glow' now)" — the surviving overlay after this task is `ai` only, not `glow`. The comment should read: `// - boolFlagOverlayMap returns only the bools that still have a lib/<name>/ overlay (just "ai" now).`.

3. `internal/scaffold/template.go`:
   - In the `overlayOrder` doc comment, drop `Glow` from the listed names: `Cobra, Fang, Viper, Huh, Glamour, Wish, Log, Harmonica`.
   - In the `boolFlagOverlayMap` doc comment: change the sentence "Plan 02-05 reduced this map to a single entry (glow). Plan 03-04 adds the second surviving overlay: lib/ai/" to: `// After this quick task, only lib/ai/ survives — the lib/glow/ overlay was dropped (glow binary is redundant; glamour provides in-process markdown rendering).`.
   - Drop the `"glow": p.Glow` entry from `boolFlagOverlayMap`. The map now has only `"ai": p.AI`.
   - In the `funcMap` helper:
     - Delete the `"hasGlow"` entry.
     - In `charmPin`, delete the `case "glow": return DefaultLegacyPins.Glow` lines.
     - In the `charmLibInfo` switch:
       - Delete the `case "glow":` block entirely.
       - Delete the `case "modifiers":` block entirely.
       - Update the doc comment above the switch from "15 keys" to "13 keys" and drop `glow` and `modifiers` from the listed examples. The comment ends: `lipgloss, huh, glamour, wish, log, harmonica, cobra, fang, viper, ansi, runewidth.` (12 listed; the 13th is "bubbles" which is already there — count: bubbletea, bubbles, lipgloss, huh, glamour, wish, log, harmonica, cobra, fang, viper, ansi, runewidth = 13).

4. `internal/scaffold/resolve.go`:
   - In the bool-flag binding loop, delete the `{"glow", &p.Glow},` and `{"modifiers", &p.Modifiers},` lines.

5. `internal/scaffold/versions.go`:
   - Update the package comment to remove the `(glow)` parenthetical. The line becomes: `//     served from the github.com path (harmonica).`.
   - Delete the `Glow string` field and its 5-line comment block from `LegacyCharmPins`.
   - Delete the `Glow: "v2.1.2",` entry from `DefaultLegacyPins`.

6. `internal/prompt/catalog.go`:
   - Update the package comment from "13 charm libraries" to "11 charm libraries" (after removal: bubbles, bubbletea, cobra, fang, glamour, harmonica, huh, lipgloss, log, viper, wish = 11).
   - Delete the `glow` and `modifiers` entries from `LibCatalog`. The new count is 11.
   - Delete the `"glow": "Glow",` entry from `libBoolMirror`.

7. `internal/prompt/huh.go`:
   - In `setBoolFieldByName`, delete the `case "Glow": p.Glow = val` lines.

8. `internal/prompt/huh_test.go`:
   - In `boolFieldByName`, delete the `case "Glow":` lines.

9. `internal/scaffold/resolve_test.go`:
   - In `newResolveCmd`, delete the `pf.Bool("glow", ...)` and `pf.Bool("modifiers", ...)` registrations.
   - In `TestResolveFlags_AllBoolsBind`:
     - Delete `--glow` and `--modifiers` from the flags slice.
     - Delete the `{"Glow", p.Glow},` and `{"Modifiers", p.Modifiers},` lines from the `checks` table.

10. `internal/scaffold/project_test.go`:
    - In the file header comment, drop `Glow` from the listed bool names: `(Cobra, Fang, Viper, Huh, Glamour, Wish, Log, Harmonica)`.
    - In `TestProject_AllLibs_AllBoolsSet`:
      - Delete the `Glow: true,` line.
      - Update the `want` slice to drop `"glow"`: `{"cobra", "fang", "glamour", "harmonica", "huh", "log", "viper", "wish"}`.

11. `internal/scaffold/agents_test.go`:
    - In `TestAGENTSmd_NoANSI`, delete the `Glow: true,` line.
    - In `TestCharmLibInfo_AllFifteen`:
      - Rename to `TestCharmLibInfo_AllThirteen`.
      - Update the `wantKeys` slice: drop `"glow"` and `"modifiers"`. The new slice has 13 entries.

Commit this task atomically: `refactor(quick-260604-7jt): remove --glow and --modifiers flags from source code and lookup tables`. Use `go build ./...` to confirm compilation before committing. Do NOT run the test suite yet — Task 2 changes templates and tests, so the suite will not be green until then.
  </action>
  <verify>
    <automated>go build ./...</automated>
  </verify>
  <done>Source compiles cleanly; `grep -n -i 'glow\|modifier' cmd/new.go internal/scaffold/project.go internal/scaffold/template.go internal/scaffold/resolve.go internal/scaffold/versions.go internal/prompt/catalog.go internal/prompt/huh.go internal/prompt/huh_test.go internal/scaffold/resolve_test.go internal/scaffold/project_test.go internal/scaffold/agents_test.go | grep -v '^//'` returns zero matches (excluding comment-only lines). All Project.Glow and Project.Modifiers references are gone from non-test code.</note>

Note: the integration_test.go and grep_test.go still contain glow references (they will be cleaned in Task 2) — that is acceptable; Task 1's verify only checks the files Task 1 touches.</done>
</task>

<task type="auto">
  <name>Task 2: Remove readme subcommand templates, lib/glow/ overlay, and update related templates + tests</name>
  <files>internal/scaffold/templates/variant_cli/internal/cmd/root.go.tmpl, internal/scaffold/templates/variant_all/internal/cmd/root.go.tmpl, internal/scaffold/templates/variant_cli/internal/cmd/readme.go.tmpl, internal/scaffold/templates/variant_all/internal/cmd/readme.go.tmpl, internal/scaffold/templates/_base/README.md.tmpl, internal/scaffold/templates/lib/ai/AGENTS.md.tmpl, internal/scaffold/templates/lib/glow/README.glow.md.tmpl, internal/scaffold/integration_test.go, internal/scaffold/grep_test.go</files>
  <action>
1. `internal/scaffold/templates/variant_cli/internal/cmd/readme.go.tmpl`:
   - Delete the file entirely. (The `readme` subcommand is gone because its only motivation was the glow shell-out; glamour-rendered readme can be re-added as a separate quick task if the user wants it.)

2. `internal/scaffold/templates/variant_all/internal/cmd/readme.go.tmpl`:
   - Delete the file entirely (same reason as variant_cli).

3. `internal/scaffold/templates/variant_cli/internal/cmd/root.go.tmpl`:
   - Update the doc comment to drop the readme line: `// hello is always present; ssh is added when --wish. viper is wired via PersistentPreRunE when --viper is set.`
   - Delete the `{{- if or (hasGlamour .) (hasGlow .)}} ... root.AddCommand(readmeCmd) ... {{- end}}` block (3 lines).

4. `internal/scaffold/templates/variant_all/internal/cmd/root.go.tmpl`:
   - Update the doc comment to drop the readme line: `// The hello subcommand is always present; tui is added when --bubbletea; ssh is added when --wish. viper is wired via PersistentPreRunE when --viper is set.`
   - Delete the `{{- if or (hasGlamour .) (hasGlow .)}} ... root.AddCommand(readmeCmd) ... {{- end}}` block (3 lines).

5. `internal/scaffold/templates/_base/README.md.tmpl`:
   - Delete the `{{ if hasGlow . -}} ... {{- end}}` block that contains the `glow README.md` bullet (4 lines: the `if`, the bullet, and the closing `{{- end}}` plus the trailing newline).

6. `internal/scaffold/templates/lib/ai/AGENTS.md.tmpl`:
   - Update the prose to reflect the new state. Change: "the two surviving `lib/<lib>/` overlays (`lib/glow/`, `lib/ai/`)" to: "the one surviving `lib/<lib>/` overlay (`lib/ai/`)".
   - Update the surrounding sentence: "...every charm lib's wiring inlined behind per-lib `has<Lib>` blocks in the variant files), and the one surviving `lib/<lib>/` overlay (`lib/ai/`); see `internal/scaffold/templates/` in the spin source for the canonical set."

7. `internal/scaffold/templates/lib/glow/README.glow.md.tmpl`:
   - Delete the file entirely.
   - Also delete the now-empty `internal/scaffold/templates/lib/glow/` directory (use `rmdir` after file removal, OR leave the empty directory and accept the embed walker will silently skip it — empty subdirs cause no harm because `//go:embed all:templates` walks and an empty dir is fine. If `gofumpt` / `go vet` complains, run `rmdir` to clean up; do not leave it as a placeholder).

8. `internal/scaffold/integration_test.go`:
   - In `TestIntegrationScaffold_CLIAllLibs` (the "all-flags scaffold test" referenced in the constraints — this is the one that exercises --cobra --fang --lipgloss --glamour --wish --log --viper):
     - Delete `filepath.Join("internal", "cmd", "readme.go")` from the `wantFiles` slice. The expected files are now: main.go, root.go, hello.go, ssh.go, styles.go.
     - Delete the entire `// Build the scaffolded CLI and exercise hello + readme subcommands.` block: from the `cliBin :=` line through the `t.Errorf("readme output missing...")` line. This block runs `readme` which no longer exists. The test should no longer exec `readme`; the structure assertion is sufficient.
   - In `TestIntegrationScaffold_AllVariant`:
     - Delete `filepath.Join("internal", "cmd", "readme.go")` from the `wantFiles` slice. The expected files are now: main.go, internal/app/{app,update,view}.go, internal/cmd/{root,hello,ssh,tui}.go, internal/ui/styles.go.
     - Update the comment that mentions "the merged tree contains internal/app/*.go AND internal/cmd/*.go" — keep it, it still applies.
     - Update the comment that says "the root help lists all 4 subcommands (tui, hello, readme, ssh)" to: "the root help lists all 3 subcommands (tui, hello, ssh)".
     - Update the loop: change `for _, sub := range []string{"tui", "hello", "readme", "ssh"}` to `for _, sub := range []string{"tui", "hello", "ssh"}`.
     - Delete the entire `// hello and readme subcommands work end-to-end` block. After the help loop, the test should end. (The hello-and-readme block is the `hello := exec.Command(allBin, "hello", "world")` plus its assertions.)
   - In `assertGoModFullTUI`'s v1-leak deny-list (the `[]string` of forbidden paths), keep `"github.com/charmbracelet/glow"` — that is still a valid v1 leak (a project that imports the bare `github.com/charmbracelet/glow` is using the v1 path; the v2 path is `github.com/charmbracelet/glow/v2`). The deny-list should not lose this entry.

9. `internal/scaffold/grep_test.go`:
   - Delete the `TestGrepV1Leaks_AllowsGlowV2` function entirely.
   - In the doc comment for `TestGrepV1Leaks_AllowsHarmonica` (around line 110–115), remove the "Companion test TestGrepV1Leaks_AllowsGlowV2" sentence and any other mention of glow in the file's comments. The remaining test (allows harmonica) does not need a glow companion.

Commit this task atomically: `refactor(quick-260604-7jt): remove readme subcommand, lib/glow/ overlay, and glow-related test fixtures`.
  </action>
  <verify>
    <automated>go build ./... && go test -count=1 -timeout 300s ./internal/scaffold/... ./internal/prompt/... ./cmd/...</automated>
  </verify>
  <done>All tests in ./internal/scaffold/..., ./internal/prompt/..., ./cmd/... pass. The lib/glow/ directory is removed. The readme.go.tmpl files for both variants are removed. The integration tests' structure assertions and help-output assertions match the new tree (no readme.go in internal/cmd, no readme in --help output).</done>
</task>

</tasks>

<verification>
After both tasks complete, the change set is:

1. `git grep -n 'glow\|modifier' -- ':!*.md' ':!CLAUDE.md' ':!scripts/check-v1-leaks.sh' ':!.planning' ':!.git'` returns matches ONLY in:
   - The `"github.com/charmbracelet/glow"` entry in integration_test.go (intentional — still a v1 leak to catch)
   - The `v1 leak` deny-list in scripts/check-v1-leaks.sh (intentional — catches projects that accidentally use the bare v1 path)
   - No other source files
2. `go build ./...` succeeds
3. `go test -count=1 -timeout 300s ./internal/scaffold/... ./internal/prompt/... ./cmd/...` succeeds
4. `go test -count=1 -timeout 300s ./...` (full suite) succeeds
5. `go run . new myapp-test --tui --bubbletea --glamour` works (glamour-rendered readme is no longer wired, but the project still builds and the glamour lib is still scaffolded)
6. `go run . new --help` does not mention `--glow` or `--modifiers`
</verification>

<success_criteria>
- The `--glow` and `--modifiers` flags are gone from the CLI surface (no `pf.Bool("glow"...)` or `pf.Bool("modifiers"...)` anywhere in cmd/ or internal/scaffold/)
- The `Project.Glow` and `Project.Modifiers` fields are gone; the type still compiles
- `charmLibInfo` returns non-empty strings for exactly 13 library keys (the 15 from before, minus glow and modifiers)
- The `readme` subcommand is gone from all variants; the corresponding `.tmpl` files are deleted
- `lib/glow/` overlay is gone; the overlay walker only walks `_base`, `variant_<type>`, and `lib/ai/` (when --ai is set)
- `_base/README.md.tmpl` no longer references the glow binary
- `lib/ai/AGENTS.md.tmpl` prose reflects the single surviving `lib/<lib>/` overlay (lib/ai/)
- All tests under ./internal/scaffold/..., ./internal/prompt/..., ./cmd/... pass
- Full test suite (`go test ./...`) passes
</success_criteria>

<output>
Skip — this is a quick plan; the orchestrator does not require a SUMMARY.md for quick tasks per GSD quick-mode conventions.
</output>
