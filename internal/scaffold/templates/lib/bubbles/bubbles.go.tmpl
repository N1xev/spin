// bubbles v2 spinner is wired in variant_tui/main.go.tmpl via conditional
// template blocks (the hasBubbles helper gates the wiring). This file is
// a placeholder so the overlay walker emits a bubbles.go in the
// scaffolded project root; the generated main.go does not import it.
package main
