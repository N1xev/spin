// Code generated by spin {{.SpinVer}}. DO NOT EDIT.

package main

import "charm.land/huh/v2"

// huhExample runs a small interactive form: a text input and a confirm.
// It is invoked from main.go when the user presses `?` (see the
// variant_tui/main.go.tmpl keybinding wiring).
//
// huh forms are accessible and work in any TTY; the framework is the
// canonical way to ask the user a question in a charmbracelet v2 TUI.
func huhExample() error {
	var name string
	var cool bool
	form := huh.NewForm(
		huh.NewGroup(
			huh.NewInput().
				Title("Your name?").
				Value(&name),
			huh.NewConfirm().
				Title("Are you cool?").
				Value(&cool),
		),
	)
	return form.Run()
}
