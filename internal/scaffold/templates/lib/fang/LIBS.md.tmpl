// fang ships in Phase 2 (forward-compat placeholder).
// This LIBS.md.tmpl is the marker that the overlay walker emits so the
// scaffolded project shows the lib was selected. The file is named
// LIBS.md (not README.md) so it does not clobber the base README.md
// produced by templates/_base/README.md.tmpl. WR-001.
