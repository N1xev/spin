// wish ships in Phase 2 (forward-compat placeholder).
// This README.md.tmpl is the marker that the overlay walker emits so the
// scaffolded project shows the lib was selected. Real content lands in
// the corresponding phase.
