// Code generated by spin {{.SpinVer}}. DO NOT EDIT.

package main

import (
	"time"

	"github.com/charmbracelet/harmonica"
)

// harmonicaExample returns a tea.Cmd that runs a 60-frame spring
// animation from 0.0 to 100.0 and reports the current position via
// tea.Msg on every tick. Surfaces as a TUI keybinding on `s`.
//
// Note: harmonica is one of the two charmbracelet libs still on the
// github.com path (the other is glow). The v1-leak grep suite allows
// it explicitly per scripts/check-v1-leaks.sh.
func harmonicaExample() tea.Cmd {
	const fps = 60
	const target = 100.0
	const frames = 60
	spring := harmonica.NewSpring(harmonica.FPS(fps), 6.0, 0.5)
	pos := 0.0
	vel := 0.0
	return tea.Tick(time.Second/fps, func(time.Time) tea.Msg {
		if frames <= 0 {
			return nil
		}
		return harmonicaTickMsg{pos: spring.Update(pos, vel, target)}
	})
}

// harmonicaTickMsg carries the spring position on each tick. The TUI's
// Update() pattern-matches on this type to advance the counter.
type harmonicaTickMsg struct{ pos float64 }
