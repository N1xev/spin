// bubbletea v2 wiring is in variant_tui/main.go.tmpl via conditional
// template blocks (the hasBubbletea helper gates the wiring). This file is
// a placeholder so the overlay walker emits a bubbletea.go in the
// scaffolded project root; the generated main.go does not import it.
package main
