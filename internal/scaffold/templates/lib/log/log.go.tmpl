// Code generated by spin {{.SpinVer}}. DO NOT EDIT.

package main

import (
	"os"

	"charm.land/log/v2"
)

// logSetup configures the global charm/log v2 default logger so any
// charm library that calls log.Info / log.Warn / log.Error writes to
// the same destination. Surfaces as a setupLogger() call in main().
func logSetup() {
	logger := log.New(os.Stderr)
	log.SetDefault(logger)
	log.SetLevel(log.InfoLevel)
}

// logExample demonstrates the minimal usage: a structured key/value log
// line. The charm/log API is slog-compatible but colorizes levels and
// renders key/value pairs with a tasteful prefix.
func logExample() {
	log.Info("hello from charm log",
		"key", "value",
		"answer", 42,
	)
}
