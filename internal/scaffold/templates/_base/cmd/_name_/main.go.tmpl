// No-op default main.go. Each variant (variant_tui, variant_cli,
// variant_all) replaces this file with a real `package main` entry
// point. This file exists so the embed walker emits a `main.go` at
// the canonical Go path `cmd/<name>/main.go` even for an empty
// scaffold — see Plan 02-05 (Task 5) for the rationale.
package main
